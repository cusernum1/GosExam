# ГЭ 7. Дифференциальные уравнения

Файл подготовлен как отдельный экзаменационный конспект по матричной экспоненте, линейным ОДУ, задаче Коши и численным методам решения ОДУ.

---

## 1. Матричная экспонента

Матричная экспонента квадратной матрицы `A` определяется рядом:

$$
e^{At}=I+At+\frac{A^2t^2}{2!}+\frac{A^3t^3}{3!}+\dots
$$

или:

$$
e^{At}=\sum_{k=0}^{\infty}\frac{A^kt^k}{k!}.
$$

Здесь `I` - единичная матрица.

### Сходимость ряда

Ряд, определяющий матричную экспоненту, сходится при любой квадратной матрице `A` и любом конечном `t`.

Идея доказательства: если использовать согласованную матричную норму, то:

$$
\left\|\frac{A^kt^k}{k!}\right\|
\le
\frac{\|A\|^k|t|^k}{k!}.
$$

Числовой ряд:

$$
\sum_{k=0}^{\infty}\frac{\|A\|^k|t|^k}{k!}
$$

сходится как ряд для экспоненты. Поэтому матричный ряд сходится абсолютно.

### Свойства

Основные свойства:

$$
e^{A0}=I.
$$

$$
\frac{d}{dt}e^{At}=Ae^{At}=e^{At}A.
$$

Если `A` и `B` коммутируют, то:

$$
e^{A+B}=e^Ae^B.
$$

Для системы:

$$
x'=Ax
$$

решение имеет вид:

$$
x(t)=e^{A(t-t_0)}x(t_0).
$$

---

## 2. Метод вариации произвольных постоянных

Рассмотрим линейное неоднородное дифференциальное уравнение `n`-го порядка:

$$
y^{(n)}+a_1(x)y^{(n-1)}+\dots+a_n(x)y=f(x).
$$

Пусть:

$$
y_1,y_2,\dots,y_n
$$

образуют фундаментальную систему решений соответствующего однородного уравнения.

Тогда решение неоднородного уравнения ищется в виде:

$$
y=C_1(x)y_1+C_2(x)y_2+\dots+C_n(x)y_n.
$$

Функции `C_i(x)` находятся из системы Лагранжа:

$$
C_1'y_1+C_2'y_2+\dots+C_n'y_n=0.
$$

$$
C_1'y_1'+C_2'y_2'+\dots+C_n'y_n'=0.
$$

И так далее до предпоследнего уравнения, а последнее:

$$
C_1'y_1^{(n-1)}+\dots+C_n'y_n^{(n-1)}=f(x).
$$

После нахождения `C_i'(x)` их интегрируют.

Смысл метода: постоянные в общем решении однородного уравнения заменяются функциями.

---

## 3. Приемы вычисления несобственных интегралов

Несобственный интеграл возникает, если:

- промежуток интегрирования бесконечен;
- подынтегральная функция неограничена;
- внутри промежутка есть особая точка.

Основной прием - переход к пределу:

$$
\int_a^{+\infty}f(x)dx=
\lim_{R\to+\infty}\int_a^R f(x)dx.
$$

Если особенность в точке `b`:

$$
\int_a^b f(x)dx=
\lim_{\varepsilon\to0+}\int_a^{b-\varepsilon}f(x)dx.
$$

Приемы:

1. Разбиение интеграла.
2. Замена переменной.
3. Выделение главной особенности.
4. Сравнение с эталонными интегралами.
5. Интегрирование по частям.
6. Численное интегрирование после устранения особенности.

---

## 4. Задача Коши для ОДУ

Задача Коши для уравнения первого порядка:

$$
y'=f(x,y), \qquad y(x_0)=y_0.
$$

Для системы:

$$
x'=f(t,x), \qquad x(t_0)=x_0.
$$

Задача состоит в нахождении решения, удовлетворяющего начальному условию.

---

## 5. Теорема существования и единственности

Одна из классических формулировок:

Если функция `f(x,y)` непрерывна в некоторой области и удовлетворяет условию Липшица по переменной `y`, то задача Коши:

$$
y'=f(x,y), \qquad y(x_0)=y_0
$$

имеет единственное локальное решение.

Условие Липшица:

$$
|f(x,y_1)-f(x,y_2)|\le L|y_1-y_2|.
$$

Смысл: правая часть не должна слишком резко меняться по `y`.

---

## 6. Группы методов решения задачи Коши

Методы делятся на:

- аналитические;
- численные.

Численные методы:

| Группа | Примеры |
|---|---|
| Одношаговые | Эйлер, модификации Эйлера, Рунге-Кутта |
| Многошаговые | Адамс, Милн |
| Явные | новое значение выражается явно |
| Неявные | новое значение находится из уравнения |
| С постоянным шагом | шаг фиксирован |
| С переменным шагом | шаг выбирается автоматически |

---

## 7. Метод Эйлера

Задача:

$$
y'=f(x,y), \qquad y(x_0)=y_0.
$$

Сетка:

$$
x_{k+1}=x_k+h.
$$

Формула Эйлера:

$$
y_{k+1}=y_k+h f(x_k,y_k).
$$

Метод использует касательную в текущей точке.

### Локальная погрешность

Локальная погрешность метода Эйлера имеет порядок:

```text
O(h^2)
```

Это погрешность одного шага при условии, что предыдущая точка известна точно.

### Глобальная погрешность

Глобальная погрешность на конечном отрезке имеет порядок:

```text
O(h)
```

Метод Эйлера является методом первого порядка.

---

## 8. Модификации метода Эйлера

### Метод Эйлера с пересчетом

Прогноз:

$$
y_{k+1}^{*}=y_k+h f(x_k,y_k).
$$

Коррекция:

$$
y_{k+1}=y_k+\frac{h}{2}
\left(f(x_k,y_k)+f(x_{k+1},y_{k+1}^{*})\right).
$$

Порядок глобальной погрешности:

```text
O(h^2)
```

### Метод средней точки

$$
y_{k+1}=y_k+h f\left(x_k+\frac{h}{2},y_k+\frac{h}{2}f(x_k,y_k)\right).
$$

Это метод второго порядка.

---

## 9. Общая схема методов Рунге-Кутты

Методы Рунге-Кутты вычисляют новое значение через несколько промежуточных наклонов.

Общий вид:

$$
y_{k+1}=y_k+h\sum_{i=1}^{s}b_iK_i.
$$

Промежуточные значения:

$$
K_i=f\left(x_k+c_ih,\; y_k+h\sum_{j=1}^{s}a_{ij}K_j\right).
$$

Классический метод Рунге-Кутты четвертого порядка:

$$
k_1=f(x_k,y_k).
$$

$$
k_2=f\left(x_k+\frac{h}{2},y_k+\frac{h}{2}k_1\right).
$$

$$
k_3=f\left(x_k+\frac{h}{2},y_k+\frac{h}{2}k_2\right).
$$

$$
k_4=f(x_k+h,y_k+hk_3).
$$

$$
y_{k+1}=y_k+\frac{h}{6}(k_1+2k_2+2k_3+k_4).
$$

---

## 10. Правило Рунге

Правило Рунге используется для оценки погрешности и выбора шага.

Если метод имеет порядок `p`, то приближенная оценка погрешности:

$$
R=\frac{y_{h/2}-y_h}{2^p-1}.
$$

Если ошибка больше допустимой, шаг уменьшают.

Если ошибка значительно меньше допустимой, шаг можно увеличить.

---

## 11. Автоматический выбор шага

Идея:

1. Сделать шаг `h`.
2. Сделать два шага `h/2`.
3. Сравнить результаты.
4. Оценить ошибку.
5. Принять или отклонить шаг.
6. Изменить шаг.

Если ошибка мала, шаг увеличивают. Если ошибка велика, шаг уменьшают.

---

## 12. Методы Рунге-Кутты-Фелберга и Рунге-Кутты-Мерсона

Методы Рунге-Кутты-Фелберга используют вложенные формулы разных порядков, например четвертого и пятого.

Одна и та же серия вычислений дает:

- приближение меньшего порядка;
- приближение большего порядка;
- оценку погрешности.

Метод Рунге-Кутты-Мерсона также строит оценку локальной ошибки и применяется для автоматического выбора шага.

Общая идея:

```text
вычислить два близких приближения разного порядка и по их разности оценить ошибку.
```

---

## 13. Экзаменационный минимум

Нужно знать:

1. Определение матричной экспоненты.
2. Сходимость ряда для `e^{At}`.
3. Решение системы `x'=Ax`.
4. Метод вариации произвольных постоянных.
5. Постановку задачи Коши.
6. Теорему существования и единственности.
7. Метод Эйлера и его погрешности.
8. Модификации метода Эйлера.
9. Общую схему Рунге-Кутты.
10. Правило Рунге и автоматический выбор шага.
11. Методы Рунге-Кутты-Фелберга и Мерсона.

