# ГЭ 4. Теория вероятностей и случайные процессы

Файл подготовлен как отдельный экзаменационный конспект по случайным величинам, предельным теоремам и основным случайным процессам.

---

## 1. Случайная величина

Случайная величина - числовая функция, заданная на пространстве элементарных исходов.

Если каждому исходу `omega` ставится в соответствие число `X(omega)`, то `X` является случайной величиной.

Случайные величины бывают:

- дискретные;
- непрерывные;
- смешанные.

---

## 2. Функция распределения

Функция распределения случайной величины `X`:

$$
F(x)=P(X\le x).
$$

Свойства:

1. `0 <= F(x) <= 1`.
2. `F(x)` не убывает.
3. `F(x)` непрерывна справа.
4. При `x -> -infinity` функция стремится к `0`.
5. При `x -> +infinity` функция стремится к `1`.

---

## 3. Непрерывная случайная величина и плотность

Случайная величина называется непрерывной, если существует функция плотности `f(x)` такая, что:

$$
F(x)=\int_{-\infty}^{x}f(t)dt.
$$

Свойства плотности:

$$
f(x)\ge 0.
$$

$$
\int_{-\infty}^{+\infty}f(x)dx=1.
$$

Вероятность попадания в интервал:

$$
P(a<X<b)=\int_a^b f(x)dx.
$$

---

## 4. Примеры непрерывных распределений

Равномерное распределение на `[a,b]`:

$$
f(x)=\frac{1}{b-a}
$$

при `a <= x <= b`.

Экспоненциальное распределение:

$$
f(x)=\lambda e^{-\lambda x}, \qquad x\ge 0.
$$

Нормальное распределение:

$$
f(x)=\frac{1}{\sigma\sqrt{2\pi}}
e^{-(x-a)^2/(2\sigma^2)}.
$$

Здесь `a` - математическое ожидание, `sigma^2` - дисперсия.

---

## 5. Схема Бернулли

Схема Бернулли - последовательность независимых испытаний, в каждом из которых событие наступает с вероятностью `p` и не наступает с вероятностью `q=1-p`.

Вероятность ровно `k` успехов в `n` испытаниях:

$$
P_n(k)=C_n^k p^k q^{n-k}.
$$

Это биномиальное распределение.

---

## 6. Теоремы Муавра-Лапласа

Локальная теорема Муавра-Лапласа приближает биномиальные вероятности:

$$
P_n(k)\approx
\frac{1}{\sqrt{npq}}
\varphi\left(\frac{k-np}{\sqrt{npq}}\right),
$$

где:

$$
\varphi(x)=\frac{1}{\sqrt{2\pi}}e^{-x^2/2}.
$$

Интегральная теорема:

$$
P(a\le X\le b)\approx
\Phi\left(\frac{b-np}{\sqrt{npq}}\right)
-\Phi\left(\frac{a-np}{\sqrt{npq}}\right).
$$

Эти теоремы показывают нормальное приближение биномиального распределения.

---

## 7. Теорема Пуассона

Если число испытаний велико, вероятность успеха мала, а:

$$
np\to \lambda,
$$

то биномиальное распределение приближается распределением Пуассона:

$$
P(X=k)\approx e^{-\lambda}\frac{\lambda^k}{k!}.
$$

Это используется для редких событий.

---

## 8. Числовые характеристики случайных величин

Математическое ожидание дискретной величины:

$$
E(X)=\sum x_kp_k.
$$

Для непрерывной:

$$
E(X)=\int_{-\infty}^{+\infty}xf(x)dx.
$$

Дисперсия:

$$
D(X)=E(X-E(X))^2.
$$

Удобная формула:

$$
D(X)=E(X^2)-E(X)^2.
$$

Момент порядка `k`:

$$
E(X^k).
$$

Центральный момент:

$$
E(X-E(X))^k.
$$

---

## 9. Ковариация и корреляция

Ковариация:

$$
\mathrm{cov}(X,Y)=E((X-E(X))(Y-E(Y))).
$$

Удобная формула:

$$
\mathrm{cov}(X,Y)=E(XY)-E(X)E(Y).
$$

Свойства:

- если величины независимы, ковариация равна нулю;
- ковариация показывает направление линейной связи;
- знак ковариации зависит от направления связи.

Коэффициент корреляции:

$$
r_{XY}=\frac{\mathrm{cov}(X,Y)}{\sqrt{D(X)}\sqrt{D(Y)}}.
$$

Свойства:

$$
-1\le r_{XY}\le 1.
$$

Если `r=1` или `r=-1`, имеется точная линейная зависимость.

Если `r=0`, линейная корреляция отсутствует, но зависимость вообще может существовать.

---

## 10. Закон больших чисел

Закон больших чисел утверждает, что среднее большого числа независимых наблюдений приближается к математическому ожиданию.

Если `X1,...,Xn` независимы и одинаково распределены с конечным математическим ожиданием `a`, то:

$$
\overline{X}_n\to a
$$

по вероятности.

---

## 11. Неравенство Чебышева и теорема Чебышева

Неравенство Чебышева:

$$
P(|X-E(X)|\ge \varepsilon)\le \frac{D(X)}{\varepsilon^2}.
$$

Оно показывает, что вероятность большого отклонения ограничивается дисперсией.

Теорема Чебышева является одной из форм закона больших чисел: при независимых случайных величинах с равномерно ограниченными дисперсиями среднее арифметическое отклоняется от среднего математических ожиданий с вероятностью, стремящейся к нулю.

---

## 12. Центральная предельная теорема

Центральная предельная теорема утверждает, что сумма большого числа независимых случайных величин при нормировке имеет приближенно нормальное распределение.

Для независимых одинаково распределенных величин с математическим ожиданием `a` и дисперсией `sigma^2`:

$$
\frac{X_1+\dots+X_n-na}{\sigma\sqrt{n}}
$$

сходится по распределению к стандартному нормальному закону.

---

## 13. Пуассонов поток событий

Пуассонов поток - поток случайных событий, обладающий свойствами:

- стационарность приращений;
- независимость приращений;
- ординарность;
- отсутствие последействия.

Если интенсивность потока равна `lambda`, то число событий за время `t` имеет распределение Пуассона:

$$
P(N(t)=k)=e^{-\lambda t}\frac{(\lambda t)^k}{k!}.
$$

Время между соседними событиями имеет экспоненциальное распределение с параметром `lambda`.

---

## 14. Гауссовы процессы

Случайный процесс называется гауссовым, если любое конечное множество его значений имеет совместное нормальное распределение.

Гауссов процесс задается:

- математическим ожиданием;
- ковариационной функцией.

Ковариационная функция:

$$
K(s,t)=\mathrm{cov}(X(s),X(t)).
$$

---

## 15. Винеровский процесс

Винеровский процесс `W(t)` - гауссов процесс со свойствами:

1. `W(0)=0`.
2. Приращения независимы.
3. Приращение `W(t)-W(s)` имеет нормальное распределение с математическим ожиданием `0` и дисперсией `t-s`.
4. Траектории непрерывны с вероятностью 1.

Свойства траекторий:

- почти наверное непрерывны;
- почти наверное нигде не дифференцируемы;
- имеют случайные колебания на любом интервале.

---

## 16. Белый шум

Белый шум - идеализированный случайный процесс с нулевым средним и некоррелированными значениями в разные моменты времени.

Формально белый шум часто рассматривается как обобщенная производная винеровского процесса.

В прикладных задачах белый шум моделирует случайные быстрые возмущения.

---

## 17. Марковский процесс и цепи Маркова

Марковский процесс обладает свойством отсутствия памяти:

```text
будущее при известном настоящем не зависит от прошлого.
```

Цепь Маркова - марковский процесс с дискретным временем и дискретным множеством состояний.

Переходные вероятности:

$$
p_{ij}=P(X_{n+1}=j\mid X_n=i).
$$

Матрица переходных вероятностей:

$$
P=(p_{ij}).
$$

Сумма элементов каждой строки равна `1`.

Распределение через один шаг:

$$
\pi_{n+1}=\pi_n P.
$$

---

## 18. Экзаменационный минимум

Нужно знать:

1. Случайную величину, функцию распределения, плотность.
2. Равномерное, экспоненциальное, нормальное распределения.
3. Схему Бернулли.
4. Теоремы Муавра-Лапласа и Пуассона.
5. Математическое ожидание, дисперсию, моменты.
6. Ковариацию и корреляцию.
7. Закон больших чисел, Чебышева, ЦПТ.
8. Пуассонов поток.
9. Гауссовы процессы, винеровский процесс, белый шум.
10. Марковские процессы и цепи Маркова.

