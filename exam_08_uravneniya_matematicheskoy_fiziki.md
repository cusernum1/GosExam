# ГЭ 8. Уравнения математической физики

Файл подготовлен как отдельный экзаменационный конспект по волновому уравнению, теплопроводности и гармоническим функциям.

---

## 1. Задача Коши для уравнения колебания струны

Уравнение колебания бесконечной струны:

$$
u_{tt}=a^2u_{xx}, \qquad -\infty<x<+\infty,\qquad t>0.
$$

Начальные условия:

$$
u(x,0)=\varphi(x).
$$

$$
u_t(x,0)=\psi(x).
$$

Здесь `u(x,t)` - отклонение струны, `a` - скорость распространения волн.

---

## 2. Метод бегущих волн

Общее решение одномерного волнового уравнения имеет вид:

$$
u(x,t)=F(x-at)+G(x+at).
$$

Функция `F(x-at)` описывает волну, бегущую вправо.

Функция `G(x+at)` описывает волну, бегущую влево.

По начальным условиям получают формулу Даламбера:

$$
u(x,t)=\frac{\varphi(x-at)+\varphi(x+at)}{2}
+\frac{1}{2a}\int_{x-at}^{x+at}\psi(s)ds.
$$

Смысл: значение в точке зависит от начальных данных на отрезке влияния `[x-at, x+at]`.

---

## 3. Смешанная задача о колебаниях струны

Для конечной струны:

$$
u_{tt}=a^2u_{xx}, \qquad 0<x<l,\qquad t>0.
$$

Граничные условия закрепленных концов:

$$
u(0,t)=0, \qquad u(l,t)=0.
$$

Начальные условия:

$$
u(x,0)=\varphi(x), \qquad u_t(x,0)=\psi(x).
$$

---

## 4. Метод разделения переменных для струны

Ищем решение в виде:

$$
u(x,t)=X(x)T(t).
$$

После подстановки:

$$
XT''=a^2X''T.
$$

Делим:

$$
\frac{T''}{a^2T}=\frac{X''}{X}=-\lambda.
$$

Получаем задачи:

$$
X''+\lambda X=0,\qquad X(0)=X(l)=0.
$$

Собственные значения:

$$
\lambda_n=\left(\frac{n\pi}{l}\right)^2.
$$

Собственные функции:

$$
X_n(x)=\sin\frac{n\pi x}{l}.
$$

Временная часть:

$$
T_n(t)=A_n\cos\frac{an\pi t}{l}+B_n\sin\frac{an\pi t}{l}.
$$

Итоговое решение:

$$
u(x,t)=\sum_{n=1}^{\infty}
\left(A_n\cos\frac{an\pi t}{l}+B_n\sin\frac{an\pi t}{l}\right)
\sin\frac{n\pi x}{l}.
$$

Коэффициенты находятся из рядов Фурье начальных условий:

$$
A_n=\frac{2}{l}\int_0^l\varphi(x)\sin\frac{n\pi x}{l}dx.
$$

$$
B_n=\frac{2}{an\pi}\int_0^l\psi(x)\sin\frac{n\pi x}{l}dx.
$$

---

## 5. Теорема о максимуме и минимуме для теплопроводности

Уравнение теплопроводности:

$$
u_t=a^2u_{xx}.
$$

Смысл теоремы максимума: решение уравнения теплопроводности не может достигать строгого внутреннего максимума или минимума в области при `t>0`, если оно не является постоянным.

Для прямоугольника:

$$
0\le x\le l,\qquad 0\le t\le T
$$

максимум и минимум достигаются на параболической границе:

- при `t=0`;
- на боковых сторонах `x=0` и `x=l`.

Следствия:

1. Единственность решения смешанной задачи.
2. Непрерывная зависимость от начальных и граничных данных.
3. Сохранение неотрицательности решения при неотрицательных данных.

---

## 6. Задача Коши для уравнения теплопроводности

На всей прямой:

$$
u_t=a^2u_{xx},\qquad -\infty<x<+\infty,\qquad t>0.
$$

Начальное условие:

$$
u(x,0)=\varphi(x).
$$

Решение задается интегралом Пуассона:

$$
u(x,t)=\frac{1}{2a\sqrt{\pi t}}
\int_{-\infty}^{+\infty}
e^{-(x-\xi)^2/(4a^2t)}\varphi(\xi)d\xi.
$$

Тепловое ядро:

$$
\frac{1}{2a\sqrt{\pi t}}e^{-(x-\xi)^2/(4a^2t)}.
$$

Смысл: начальное распределение температуры сглаживается гауссовым ядром.

---

## 7. Основная интегральная формула для гармонических функций

Гармоническая функция удовлетворяет уравнению Лапласа:

$$
\Delta u=0.
$$

Для гармонических функций важны интегральные формулы, выражающие значение функции внутри области через значения на границе.

Для круга используется интеграл Пуассона для гармонической функции.

Если `u` гармонична в круге радиуса `R`, а на границе:

$$
u(R,\varphi)=f(\varphi),
$$

то внутри круга:

$$
u(r,\varphi)=
\frac{1}{2\pi}\int_0^{2\pi}
\frac{R^2-r^2}{R^2-2Rr\cos(\varphi-\theta)+r^2}
f(\theta)d\theta.
$$

Эта формула показывает, что гармоническая функция внутри круга полностью определяется своими граничными значениями.

---

## 8. Экзаменационный минимум

Нужно знать:

1. Волновое уравнение и задачу Коши.
2. Метод бегущих волн и формулу Даламбера.
3. Смешанную задачу для струны.
4. Метод разделения переменных.
5. Собственные функции `sin(n*pi*x/l)`.
6. Теорему максимума и минимума для теплопроводности.
7. Интеграл Пуассона для теплопроводности.
8. Интегральную формулу Пуассона для гармонических функций в круге.

