# Уравнения в частных производных

Конспект для подготовки к государственному экзамену по направлению "Математика и компьютерные науки".

Материал составлен в академическом стиле: основные определения, теоремы, методы решения и подробный разбор типовых задач с изображений.

---

## 1. Используемая литература

### ЭУМД основная

Владимиров, В. С.; Жаринов, В. В. Уравнения математической физики. М.: Физматлит, 2000. 400 с. Режим доступа: http://e.lanbook.com/book/2363.

### ЭУМД дополнительная

Ильин, А. М. Уравнения математической физики. М.: Физматлит, 2009. 192 с. Режим доступа: http://e.lanbook.com/book/2181.

Блинова, И. В.; Попов, И. Ю. Простейшие уравнения математической физики. СПб.: НИУ ИТМО, 2009. 60 с. Режим доступа: http://e.lanbook.com/book/43439.

### ПУМД основная

Тихонов, А. Н.; Самарский, А. А. Уравнения математической физики. 7-е изд. М.: Издательство Московского университета; Наука, 2004. 798 с.

Олейник, О. А. Лекции об уравнениях с частными производными. 2-е изд., испр. и доп. М.: Бином. Лаборатория знаний, 2005. 260 с.

---

## 2. Основные понятия

### 2.1. Уравнение в частных производных

Уравнением в частных производных называется уравнение, связывающее неизвестную функцию нескольких независимых переменных и ее частные производные.

Например, если неизвестная функция имеет вид:

$$
u=u(x,t),
$$

то в уравнение могут входить производные:

$$
u_x, \qquad u_t, \qquad u_{xx}, \qquad u_{tt}, \qquad u_{xt}.
$$

Порядком уравнения называется наибольший порядок частной производной, входящей в уравнение.

### 2.2. Основные уравнения математической физики

Три базовых типа уравнений:

| Тип уравнения | Канонический пример | Физический смысл |
|---|---|---|
| Гиперболическое | `u_{tt}=a^2u_{xx}` | колебания струны, волны |
| Параболическое | `u_t=a^2u_{xx}` | теплопроводность, диффузия |
| Эллиптическое | `\Delta u=0` | стационарные поля, потенциалы |

### 2.3. Оператор Лапласа

В двумерном случае:

$$
\Delta u=u_{xx}+u_{yy}.
$$

В полярных координатах:

$$
\Delta u=u_{rr}+\frac{1}{r}u_r+\frac{1}{r^2}u_{\varphi\varphi}.
$$

Если решение радиально симметрично, то есть `u=u(r,t)`, то:

$$
\Delta u=u_{rr}+\frac{1}{r}u_r.
$$

---

## 3. Классификация уравнений второго порядка

Рассмотрим линейное уравнение второго порядка с двумя независимыми переменными:

$$
Au_{xx}+2Bu_{xy}+Cu_{yy}+Du_x+Eu_y+Fu=G.
$$

Тип уравнения определяется знаком выражения:

$$
B^2-AC.
$$

Если:

$$
B^2-AC>0,
$$

то уравнение гиперболического типа.

Если:

$$
B^2-AC=0,
$$

то уравнение параболического типа.

Если:

$$
B^2-AC<0,
$$

то уравнение эллиптического типа.

### 3.1. Характеристики

Характеристическое уравнение:

$$
A(dy)^2-2Bdxdy+C(dx)^2=0.
$$

Его решения дают новые переменные, в которых уравнение приводится к каноническому виду.

---

## 4. Уравнение теплопроводности

### 4.1. Основное уравнение

Одномерное уравнение теплопроводности:

$$
u_t=a^2u_{xx}.
$$

Здесь:

- `u(x,t)` - температура;
- `x` - пространственная координата;
- `t` - время;
- `a^2` - коэффициент температуропроводности.

### 4.2. Первая смешанная задача на отрезке

На отрезке `0<x<l` обычно задают:

$$
u_t=a^2u_{xx}, \qquad 0<x<l, \qquad t>0.
$$

Начальное условие:

$$
u(x,0)=\varphi(x).
$$

Граничные условия первого рода:

$$
u(0,t)=0, \qquad u(l,t)=0.
$$

Решение ищется в виде ряда Фурье:

$$
u(x,t)=\sum_{n=1}^{\infty} b_n e^{-a^2n^2\pi^2t/l^2}\sin\frac{n\pi x}{l}.
$$

Коэффициенты:

$$
b_n=\frac{2}{l}\int_0^l \varphi(x)\sin\frac{n\pi x}{l}\,dx.
$$

### 4.3. Теорема о максимуме и минимуме

Рассмотрим уравнение теплопроводности:

$$
u_t=a^2\Delta u.
$$

Идея теоремы максимума: если функция `u` удовлетворяет уравнению теплопроводности в цилиндрической области, то ее наибольшее и наименьшее значения достигаются на параболической границе области, а не во внутренних точках при `t>0`.

Для одномерной задачи на отрезке параболическая граница состоит из:

- начального отрезка `t=0`;
- боковой границы `x=0`;
- боковой границы `x=l`.

Если:

$$
u_t-a^2u_{xx}=0,
$$

то максимум и минимум решения в замкнутом прямоугольнике:

$$
0\le x\le l, \qquad 0\le t\le T
$$

достигаются при `t=0` или на боковых границах `x=0`, `x=l`.

### 4.4. Следствия теоремы максимума

Главные следствия:

1. Единственность решения первой смешанной задачи.
2. Непрерывная зависимость решения от начальных и граничных данных.
3. Если начальные и граничные данные неотрицательны, то решение также неотрицательно.

### 4.5. Почему это важно

Если для одной и той же задачи существуют два решения `u_1` и `u_2`, то их разность:

$$
w=u_1-u_2
$$

удовлетворяет однородной задаче с нулевыми начальными и граничными условиями.

По теореме максимума:

$$
w=0.
$$

Значит:

$$
u_1=u_2.
$$

То есть решение единственно.

---

## 5. Задача Коши для уравнения теплопроводности. Интеграл Пуассона

### 5.1. Постановка задачи Коши

Задача Коши для уравнения теплопроводности на всей прямой имеет вид:

$$
u_t=a^2u_{xx}, \qquad -\infty<x<+\infty, \qquad t>0.
$$

Начальное условие:

$$
u(x,0)=\varphi(x).
$$

### 5.2. Интеграл Пуассона

Решение задачи Коши задается формулой:

$$
u(x,t)=\frac{1}{2a\sqrt{\pi t}}\int_{-\infty}^{+\infty}
e^{-(x-\xi)^2/(4a^2t)}\varphi(\xi)\,d\xi.
$$

Эквивалентная запись:

$$
u(x,t)=\frac{1}{\sqrt{4\pi a^2t}}\int_{-\infty}^{+\infty}
e^{-(x-\xi)^2/(4a^2t)}\varphi(\xi)\,d\xi.
$$

Функция:

$$
\frac{1}{\sqrt{4\pi a^2t}}e^{-(x-\xi)^2/(4a^2t)}
$$

называется тепловым ядром.

### 5.3. Смысл формулы Пуассона

Значение температуры в точке `x` в момент времени `t` получается как усреднение начальной температуры `\varphi(\xi)` с гауссовым весом.

Чем больше `t`, тем сильнее начальное распределение сглаживается.

---

## 6. Волновое уравнение на отрезке

### 6.1. Первая смешанная задача

Волновое уравнение:

$$
u_{tt}=a^2u_{xx}, \qquad 0<x<l, \qquad t>0.
$$

Начальные условия:

$$
u(x,0)=\varphi(x), \qquad u_t(x,0)=\psi(x).
$$

Граничные условия:

$$
u(0,t)=0, \qquad u(l,t)=0.
$$

Решение:

$$
u(x,t)=\sum_{n=1}^{\infty}
\left(A_n\cos\frac{an\pi t}{l}+B_n\sin\frac{an\pi t}{l}\right)
\sin\frac{n\pi x}{l}.
$$

Коэффициенты:

$$
A_n=\frac{2}{l}\int_0^l\varphi(x)\sin\frac{n\pi x}{l}\,dx.
$$

$$
B_n=\frac{2}{an\pi}\int_0^l\psi(x)\sin\frac{n\pi x}{l}\,dx.
$$

---

## 7. Пример 1. Волновое уравнение на отрезке

На изображении номер `9.6` является номером задачи, поэтому уравнение читается как:

$$
u_{tt}=4u_{xx}, \qquad 0<x<1, \qquad t>0.
$$

Начальные условия:

$$
u(x,0)=x(x-1), \qquad u_t(x,0)=0.
$$

Граничные условия:

$$
u(0,t)=0, \qquad u(1,t)=0.
$$

Здесь:

$$
a^2=4, \qquad a=2, \qquad l=1.
$$

Решение имеет вид:

$$
u(x,t)=\sum_{n=1}^{\infty}
A_n\cos(2n\pi t)\sin(n\pi x).
$$

Так как `u_t(x,0)=0`, все коэффициенты при синусах по времени равны нулю.

Коэффициенты:

$$
A_n=2\int_0^1 x(x-1)\sin(n\pi x)\,dx.
$$

Известно, что для функции `x(1-x)`:

$$
2\int_0^1 x(1-x)\sin(n\pi x)\,dx=
\frac{4(1-(-1)^n)}{n^3\pi^3}.
$$

Но у нас:

$$
x(x-1)=-x(1-x).
$$

Значит:

$$
A_n=-\frac{4(1-(-1)^n)}{n^3\pi^3}.
$$

Для четных `n` коэффициент равен нулю. Для нечетных `n`:

$$
A_n=-\frac{8}{n^3\pi^3}.
$$

Ответ:

$$
u(x,t)=
-\sum_{n=1}^{\infty}
\frac{4(1-(-1)^n)}{n^3\pi^3}
\cos(2n\pi t)\sin(n\pi x).
$$

Можно записать только по нечетным номерам:

$$
u(x,t)=
-\sum_{k=0}^{\infty}
\frac{8}{(2k+1)^3\pi^3}
\cos(2(2k+1)\pi t)\sin((2k+1)\pi x).
$$

---

## 8. Уравнение Лапласа в круге

### 8.1. Задача Дирихле внутри круга

В круге:

$$
0\le r<R
$$

уравнение Лапласа имеет вид:

$$
\Delta u=0.
$$

Если на границе задано:

$$
u(R,\varphi)=f(\varphi),
$$

то решение внутри круга ищется в виде:

$$
u(r,\varphi)=\frac{a_0}{2}+
\sum_{n=1}^{\infty}
\left(\frac{r}{R}\right)^n
(a_n\cos n\varphi+b_n\sin n\varphi).
$$

Коэффициенты Фурье:

$$
a_0=\frac{1}{\pi}\int_0^{2\pi}f(\varphi)\,d\varphi.
$$

$$
a_n=\frac{1}{\pi}\int_0^{2\pi}f(\varphi)\cos n\varphi\,d\varphi.
$$

$$
b_n=\frac{1}{\pi}\int_0^{2\pi}f(\varphi)\sin n\varphi\,d\varphi.
$$

### 8.2. Задача Дирихле вне круга

Для области:

$$
r>R
$$

и условия ограниченности при бесконечности решение имеет вид:

$$
u(r,\varphi)=\frac{a_0}{2}+
\sum_{n=1}^{\infty}
\left(\frac{R}{r}\right)^n
(a_n\cos n\varphi+b_n\sin n\varphi).
$$

---

## 9. Пример 2. Задача Дирихле для уравнения Лапласа в круге

### 9.1. Граница `u(1,\varphi)=3\varphi^2+\varphi+2`

Решить задачу:

$$
\Delta u=0, \qquad 0\le r<1.
$$

Граничное условие:

$$
u(1,\varphi)=3\varphi^2+\varphi+2.
$$

Важно: функция от `\varphi` на окружности рассматривается как `2\pi`-периодическое продолжение функции, заданной на отрезке `0\le\varphi<2\pi`.

Пусть:

$$
f(\varphi)=3\varphi^2+\varphi+2.
$$

Коэффициент:

$$
a_0=\frac{1}{\pi}\int_0^{2\pi}(3\varphi^2+\varphi+2)\,d\varphi.
$$

Вычисляем:

$$
a_0=8\pi^2+2\pi+4.
$$

Значит:

$$
\frac{a_0}{2}=4\pi^2+\pi+2.
$$

Для `n\ge 1`:

$$
a_n=\frac{12}{n^2}.
$$

$$
b_n=-\frac{12\pi+2}{n}.
$$

Ответ:

$$
u(r,\varphi)=4\pi^2+\pi+2+
\sum_{n=1}^{\infty}
r^n\left(\frac{12}{n^2}\cos n\varphi
-\frac{12\pi+2}{n}\sin n\varphi\right).
$$

### 9.2. Граница `u(1,\varphi)=\varphi^2+7\varphi-1`

Теперь:

$$
f(\varphi)=\varphi^2+7\varphi-1.
$$

Коэффициент:

$$
a_0=\frac{1}{\pi}\int_0^{2\pi}(\varphi^2+7\varphi-1)\,d\varphi.
$$

Получаем:

$$
a_0=\frac{8\pi^2}{3}+14\pi-2.
$$

Следовательно:

$$
\frac{a_0}{2}=\frac{4\pi^2}{3}+7\pi-1.
$$

Для `n\ge 1`:

$$
a_n=\frac{4}{n^2}.
$$

$$
b_n=-\frac{4\pi+14}{n}.
$$

Ответ:

$$
u(r,\varphi)=
\frac{4\pi^2}{3}+7\pi-1+
\sum_{n=1}^{\infty}
r^n\left(\frac{4}{n^2}\cos n\varphi
-\frac{4\pi+14}{n}\sin n\varphi\right).
$$

---

## 10. Задача Штурма-Лиувилля

### 10.1. Общая идея

Задача Штурма-Лиувилля возникает при разделении переменных в уравнениях математической физики.

Типичный вид:

$$
y''+\lambda y=0
$$

на отрезке с краевыми условиями.

Нужно найти такие значения параметра `\lambda`, при которых существуют нетривиальные решения.

Эти значения называются собственными значениями, а соответствующие функции - собственными функциями.

### 10.2. Пример 3а

Найти отличные от нуля решения:

$$
y''+\lambda y=0, \qquad \frac{\pi}{2}\le x\le \frac{5\pi}{4}.
$$

Краевые условия:

$$
y\left(\frac{\pi}{2}\right)=0,
\qquad
y'\left(\frac{5\pi}{4}\right)=0.
$$

Длина отрезка:

$$
L=\frac{5\pi}{4}-\frac{\pi}{2}=\frac{3\pi}{4}.
$$

Вводим:

$$
\xi=x-\frac{\pi}{2}.
$$

Тогда:

$$
0\le \xi\le L.
$$

Условия:

$$
y(0)=0, \qquad y'(L)=0.
$$

При `\lambda=k^2>0`:

$$
y=A\cos k\xi+B\sin k\xi.
$$

Из условия `y(0)=0` получаем:

$$
A=0.
$$

Значит:

$$
y=B\sin k\xi.
$$

Производная:

$$
y'=Bk\cos k\xi.
$$

Условие `y'(L)=0` дает:

$$
\cos kL=0.
$$

Следовательно:

$$
kL=\frac{(2n+1)\pi}{2}, \qquad n=0,1,2,\dots
$$

Отсюда:

$$
k_n=\frac{(2n+1)\pi}{2L}.
$$

Так как:

$$
L=\frac{3\pi}{4},
$$

то:

$$
k_n=\frac{2(2n+1)}{3}.
$$

Собственные значения:

$$
\lambda_n=k_n^2=\frac{4(2n+1)^2}{9}.
$$

Собственные функции:

$$
y_n(x)=C_n\sin\left(\frac{2(2n+1)}{3}\left(x-\frac{\pi}{2}\right)\right).
$$

### 10.3. Пример 3б

Задача:

$$
y''+\lambda y=0, \qquad \pi\le x\le 2\pi.
$$

Краевые условия:

$$
y(\pi)=0, \qquad y'(2\pi)=0.
$$

Длина отрезка:

$$
L=2\pi-\pi=\pi.
$$

Аналогично предыдущему примеру:

$$
kL=\frac{(2n+1)\pi}{2}.
$$

Поэтому:

$$
k_n=\frac{2n+1}{2}.
$$

Собственные значения:

$$
\lambda_n=\frac{(2n+1)^2}{4}.
$$

Собственные функции:

$$
y_n(x)=C_n\sin\left(\frac{2n+1}{2}(x-\pi)\right).
$$

---

## 11. Теплопроводность в круге

### 11.1. Радиальная задача

Если решение зависит только от радиуса `r` и времени `t`, то:

$$
u_t=a^2\left(u_{rr}+\frac{1}{r}u_r\right).
$$

Для круга радиуса `R` с условием:

$$
u(R,t)=0
$$

решение имеет вид:

$$
u(r,t)=\sum_{n=1}^{\infty}
A_n J_0\left(\alpha_n\frac{r}{R}\right)
e^{-a^2\alpha_n^2t/R^2}.
$$

Здесь:

- `J_0` - функция Бесселя нулевого порядка;
- `\alpha_n` - положительные нули функции `J_0`;
- коэффициенты находятся из разложения начальной функции по функциям Бесселя.

Формула коэффициентов:

$$
A_n=\frac{2}{R^2J_1(\alpha_n)^2}
\int_0^R f(r)J_0\left(\alpha_n\frac{r}{R}\right)r\,dr.
$$

### 11.2. Пример 4а

Задача:

$$
u_t=4\Delta u, \qquad 0\le r<2, \qquad t>0.
$$

Начальное условие:

$$
u(r,0)=4-r^2.
$$

Граничное условие:

$$
u(2,t)=0.
$$

Здесь:

$$
R=2, \qquad a^2=4.
$$

Решение:

$$
u(r,t)=\sum_{n=1}^{\infty}
A_n J_0\left(\alpha_n\frac{r}{2}\right)e^{-\alpha_n^2t}.
$$

Для начальной функции `4-r^2` коэффициенты упрощаются:

$$
A_n=\frac{32}{\alpha_n^3J_1(\alpha_n)}.
$$

Ответ:

$$
u(r,t)=
\sum_{n=1}^{\infty}
\frac{32}{\alpha_n^3J_1(\alpha_n)}
J_0\left(\alpha_n\frac{r}{2}\right)e^{-\alpha_n^2t}.
$$

### 11.3. Пример 4б

Задача:

$$
u_t=2\Delta u, \qquad 0\le r<3, \qquad t>0.
$$

Начальное условие:

$$
u(r,0)=9-r^2.
$$

Граничное условие:

$$
u(3,t)=0.
$$

Здесь:

$$
R=3, \qquad a^2=2.
$$

Решение:

$$
u(r,t)=\sum_{n=1}^{\infty}
A_n J_0\left(\alpha_n\frac{r}{3}\right)
e^{-2\alpha_n^2t/9}.
$$

Коэффициенты:

$$
A_n=\frac{72}{\alpha_n^3J_1(\alpha_n)}.
$$

Ответ:

$$
u(r,t)=
\sum_{n=1}^{\infty}
\frac{72}{\alpha_n^3J_1(\alpha_n)}
J_0\left(\alpha_n\frac{r}{3}\right)
e^{-2\alpha_n^2t/9}.
$$

---

## 12. Приведение уравнения к каноническому виду

### 12.1. Пример 5а

Уравнение:

$$
u_{xx}-12u_{xy}+36u_{yy}+2u_x-12u_y=0.
$$

Здесь:

$$
A=1, \qquad 2B=-12, \qquad C=36.
$$

Следовательно:

$$
B=-6.
$$

Дискриминант:

$$
B^2-AC=36-36=0.
$$

Уравнение параболического типа.

Характеристическое уравнение:

$$
(dy+6dx)^2=0.
$$

Отсюда:

$$
y+6x=C.
$$

Выбираем новые переменные:

$$
\xi=y+6x, \qquad \eta=x.
$$

Пусть:

$$
u(x,y)=v(\xi,\eta).
$$

Тогда уравнение приводится к виду:

$$
v_{\eta\eta}+2v_{\eta}=0.
$$

Решаем:

$$
v_{\eta\eta}+2v_{\eta}=0.
$$

Общее решение:

$$
v=F(\xi)+e^{-2\eta}G(\xi).
$$

Возвращаемся к `x`, `y`:

$$
u(x,y)=F(y+6x)+e^{-2x}G(y+6x).
$$

### 12.2. Пример 5б

Уравнение:

$$
u_{xx}-4u_{xy}+4u_{yy}+3u_x-6u_y=0.
$$

Здесь:

$$
A=1, \qquad 2B=-4, \qquad C=4.
$$

Значит:

$$
B=-2.
$$

Дискриминант:

$$
B^2-AC=4-4=0.
$$

Уравнение параболического типа.

Характеристическое уравнение:

$$
(dy+2dx)^2=0.
$$

Отсюда:

$$
y+2x=C.
$$

Берем:

$$
\xi=y+2x, \qquad \eta=x.
$$

После замены:

$$
v_{\eta\eta}+3v_{\eta}=0.
$$

Решение:

$$
v=F(\xi)+e^{-3\eta}G(\xi).
$$

Ответ:

$$
u(x,y)=F(y+2x)+e^{-3x}G(y+2x).
$$

### 12.3. Пример 5в

Уравнение:

$$
25u_{xx}+20u_{xy}+3u_{yy}=0.
$$

Здесь:

$$
A=25, \qquad 2B=20, \qquad C=3.
$$

Значит:

$$
B=10.
$$

Дискриминант:

$$
B^2-AC=100-75=25>0.
$$

Уравнение гиперболического типа.

Характеристическое уравнение:

$$
25(dy)^2-20dxdy+3(dx)^2=0.
$$

Если:

$$
p=\frac{dy}{dx},
$$

то:

$$
25p^2-20p+3=0.
$$

Корни:

$$
p_1=\frac35, \qquad p_2=\frac15.
$$

Характеристики:

$$
y-\frac35x=C_1,
\qquad
y-\frac15x=C_2.
$$

Берем:

$$
\xi=y-\frac35x,
\qquad
\eta=y-\frac15x.
$$

Канонический вид:

$$
v_{\xi\eta}=0.
$$

Общее решение:

$$
v=F(\xi)+G(\eta).
$$

Ответ:

$$
u(x,y)=F\left(y-\frac35x\right)+G\left(y-\frac15x\right).
$$

---

## 13. Гармонические функции внутри и вне круга

### 13.1. Пример 6. Гармоническая функция вне круга

Найти функцию `u`, гармоническую вне круга, если:

$$
u(4,\varphi)=4+3\sin^3\varphi+5\cos3\varphi.
$$

Используем тождество:

$$
\sin^3\varphi=\frac{3\sin\varphi-\sin3\varphi}{4}.
$$

Тогда:

$$
3\sin^3\varphi=\frac94\sin\varphi-\frac34\sin3\varphi.
$$

Граничная функция:

$$
f(\varphi)=4+\frac94\sin\varphi-\frac34\sin3\varphi+5\cos3\varphi.
$$

Для внешности круга радиуса `4`, если функция ограничена на бесконечности:

$$
u(r,\varphi)=4+
\frac{4}{r}\cdot\frac94\sin\varphi+
\left(\frac{4}{r}\right)^3
\left(5\cos3\varphi-\frac34\sin3\varphi\right).
$$

Упрощаем:

$$
u(r,\varphi)=4+\frac{9}{r}\sin\varphi+
\frac{320}{r^3}\cos3\varphi
-\frac{48}{r^3}\sin3\varphi.
$$

### 13.2. Пример 8. Гармоническая функция внутри круга

Найти функцию `u`, гармоническую внутри круга, если:

$$
u(5,\varphi)=3\cos^3\varphi+3\sin5\varphi+\cos5\varphi.
$$

Используем тождество:

$$
\cos^3\varphi=\frac{3\cos\varphi+\cos3\varphi}{4}.
$$

Тогда:

$$
3\cos^3\varphi=\frac94\cos\varphi+\frac34\cos3\varphi.
$$

Для внутренности круга радиуса `5`:

$$
u(r,\varphi)=
\left(\frac{r}{5}\right)\frac94\cos\varphi+
\left(\frac{r}{5}\right)^3\frac34\cos3\varphi+
\left(\frac{r}{5}\right)^5(3\sin5\varphi+\cos5\varphi).
$$

---

## 14. Теплопроводность на отрезке с кусочно заданной начальной функцией

### 14.1. Пример 7а

Задача:

$$
u_t=25u_{xx}, \qquad 0<x<9, \qquad t>0.
$$

Граничные условия:

$$
u(0,t)=0, \qquad u(9,t)=0.
$$

Начальное условие:

На отрезке `0\le x\le 9/2`:

$$
u(x,0)=\frac{2x^2}{9}.
$$

На отрезке `9/2<x\le 9`:

$$
u(x,0)=9-x.
$$

Здесь:

$$
l=9, \qquad a^2=25.
$$

Решение:

$$
u(x,t)=\sum_{n=1}^{\infty}
b_n e^{-25n^2\pi^2t/81}\sin\frac{n\pi x}{9}.
$$

Коэффициенты:

$$
b_n=\frac{2}{9}
\left(
\int_0^{9/2}\frac{2x^2}{9}\sin\frac{n\pi x}{9}\,dx+
\int_{9/2}^{9}(9-x)\sin\frac{n\pi x}{9}\,dx
\right).
$$

После вычисления интегралов:

$$
b_n=
\frac{54\sin(n\pi/2)}{n^2\pi^2}
+\frac{72(\cos(n\pi/2)-1)}{n^3\pi^3}.
$$

### 14.2. Пример 7б

Задача:

$$
u_t=9u_{xx}, \qquad 0<x<10, \qquad t>0.
$$

Граничные условия:

$$
u(0,t)=0, \qquad u(10,t)=0.
$$

Начальное условие:

На отрезке `0\le x\le 5`:

$$
u(x,0)=\frac{x^2}{5}.
$$

На отрезке `5<x\le 10`:

$$
u(x,0)=10-x.
$$

Здесь:

$$
l=10, \qquad a^2=9.
$$

Решение:

$$
u(x,t)=\sum_{n=1}^{\infty}
b_n e^{-9n^2\pi^2t/100}\sin\frac{n\pi x}{10}.
$$

Коэффициенты:

$$
b_n=\frac{1}{5}
\left(
\int_0^5\frac{x^2}{5}\sin\frac{n\pi x}{10}\,dx+
\int_5^{10}(10-x)\sin\frac{n\pi x}{10}\,dx
\right).
$$

После вычисления:

$$
b_n=
\frac{60\sin(n\pi/2)}{n^2\pi^2}
+\frac{80(\cos(n\pi/2)-1)}{n^3\pi^3}.
$$

---

## 15. Задача Коши для уравнения теплопроводности по формуле Пуассона

На изображении уравнение относится к теплопроводности, поэтому используется вид `u_t=ku_{xx}`. Если в записи кажется, что стоит `u_{tt}`, это было бы уже волновое уравнение и потребовало бы второго начального условия.

### 15.1. Общая формула для гауссовой начальной функции

Пусть:

$$
u_t=ku_{xx},
$$

и:

$$
u(x,0)=e^{-\alpha x^2+\beta x}.
$$

Тогда по формуле Пуассона:

$$
u(x,t)=
\frac{1}{\sqrt{1+4\alpha kt}}
e^{(-\alpha x^2+\beta x+k\beta^2t)/(1+4\alpha kt)}.
$$

### 15.2. Пример 9а

Задача:

$$
u_t=3u_{xx}.
$$

Начальное условие:

$$
u(x,0)=e^{-3x^2+2x}.
$$

Здесь:

$$
k=3, \qquad \alpha=3, \qquad \beta=2.
$$

Подставляем в формулу:

$$
u(x,t)=
\frac{1}{\sqrt{1+36t}}
e^{(-3x^2+2x+12t)/(1+36t)}.
$$

### 15.3. Пример 9б

Задача:

$$
u_t=5u_{xx}.
$$

Начальное условие:

$$
u(x,0)=e^{-2x^2-x}.
$$

Здесь:

$$
k=5, \qquad \alpha=2, \qquad \beta=-1.
$$

Получаем:

$$
u(x,t)=
\frac{1}{\sqrt{1+40t}}
e^{(-2x^2-x+5t)/(1+40t)}.
$$

---

## 16. Краткий алгоритм выбора метода

Если дано уравнение теплопроводности на отрезке с нулевыми граничными условиями:

```text
раскладываем начальную функцию в ряд Фурье по синусам
```

Если дана задача Коши для теплопроводности на всей прямой:

```text
используем интеграл Пуассона
```

Если дана задача Дирихле для круга:

```text
раскладываем граничную функцию в ряд Фурье по углу
```

Если дана радиальная задача теплопроводности в круге:

```text
используем функции Бесселя и нули J_0
```

Если дана задача Штурма-Лиувилля:

```text
ищем значения lambda, при которых краевая задача имеет нетривиальные решения
```

Если просят привести уравнение к каноническому виду:

```text
находим A, B, C, определяем тип, решаем характеристическое уравнение
```

---

## 17. Типичные ошибки

### Ошибка 1. Путать волновое уравнение и уравнение теплопроводности

Волновое уравнение:

$$
u_{tt}=a^2u_{xx}.
$$

Теплопроводность:

$$
u_t=a^2u_{xx}.
$$

Для волнового уравнения нужны два начальных условия: `u(x,0)` и `u_t(x,0)`.

Для теплопроводности обычно достаточно одного начального условия `u(x,0)`.

### Ошибка 2. Забыть граничные условия

Если:

$$
u(0,t)=u(l,t)=0,
$$

то нужно использовать синусы:

$$
\sin\frac{n\pi x}{l}.
$$

### Ошибка 3. Неверно выбрать показатель экспоненты

Для теплопроводности на отрезке:

$$
e^{-a^2n^2\pi^2t/l^2}.
$$

Для волнового уравнения вместо затухания появляются:

$$
\cos\frac{an\pi t}{l},
\qquad
\sin\frac{an\pi t}{l}.
$$

### Ошибка 4. Забыть периодичность граничной функции в круге

Функция `f(\varphi)` на окружности должна рассматриваться как `2\pi`-периодическая.

Если в условии дана функция типа `\varphi^2`, она задает периодическое продолжение на окружности, но на стыке может иметь разрыв.

### Ошибка 5. В задаче вне круга перепутать степени

Внутри круга используются степени:

$$
\left(\frac{r}{R}\right)^n.
$$

Вне круга при условии ограниченности на бесконечности используются:

$$
\left(\frac{R}{r}\right)^n.
$$

---

## 18. Экзаменационный минимум

Нужно уверенно знать:

1. Что такое уравнение в частных производных.
2. Что такое уравнение теплопроводности, волновое уравнение и уравнение Лапласа.
3. Классификацию уравнений второго порядка по знаку `B^2-AC`.
4. Теорему максимума и минимума для уравнения теплопроводности.
5. Формулу Пуассона для задачи Коши теплопроводности.
6. Метод разделения переменных.
7. Ряды Фурье по синусам для задач на отрезке.
8. Решение задачи Дирихле в круге через ряд Фурье.
9. Собственные значения и собственные функции задачи Штурма-Лиувилля.
10. Радиальную теплопроводность в круге через функции Бесселя.
11. Приведение уравнений второго порядка к каноническому виду.
12. Различие между внутренней и внешней задачей для гармонических функций.
