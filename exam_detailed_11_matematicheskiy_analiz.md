# ГЭ 11. Математический анализ: подробный ответ

Расширенный конспект по непрерывности, дифференцированию, интегралам, рядам, Фурье и пространству L2.

---

## 1. Непрерывность

Функция непрерывна в точке `a`, если:

$$
\lim_{x\to a}f(x)=f(a).
$$

Определение через epsilon-delta:

для любого `epsilon > 0` существует `delta > 0`, что из `|x-a|<delta` следует:

$$
|f(x)-f(a)|<\varepsilon.
$$

Определение через последовательности:

```text
если x_n стремится к a, то f(x_n) стремится к f(a).
```

Компакт в `R` - замкнутое и ограниченное множество. Непрерывный образ компакта является компактом. Поэтому непрерывная функция на отрезке ограничена и достигает максимума и минимума.

---

## 2. Равномерная непрерывность

Обычная непрерывность допускает, что `delta` зависит от точки.

Равномерная непрерывность требует одно `delta` для всех точек множества.

Теорема Кантора:

```text
функция, непрерывная на компакте, равномерно непрерывна на нем.
```

Пример важности: функция `1/x` непрерывна на `(0,1)`, но не равномерно непрерывна на этом интервале.

---

## 3. Производная и теорема Лагранжа

Производная:

$$
f'(x_0)=\lim_{\Delta x\to0}
\frac{f(x_0+\Delta x)-f(x_0)}{\Delta x}.
$$

Геометрический смысл: угловой коэффициент касательной.

Физический смысл: мгновенная скорость изменения.

Теорема Лагранжа:

если `f` непрерывна на `[a,b]` и дифференцируема на `(a,b)`, то существует `c`, для которого:

$$
f'(c)=\frac{f(b)-f(a)}{b-a}.
$$

Следствия:

- если производная равна нулю на интервале, функция постоянна;
- если производная положительна, функция возрастает;
- если производная отрицательна, функция убывает.

---

## 4. Интеграл Римана

Интеграл Римана определяется как предел интегральных сумм.

Непрерывная функция на отрезке интегрируема.

Формула Ньютона-Лейбница:

$$
\int_a^b f(x)dx=F(b)-F(a),
$$

где `F'(x)=f(x)`.

Если `f` интегрируема, то `|f|` тоже интегрируема, и:

$$
\left|\int_a^b f(x)dx\right|
\le
\int_a^b |f(x)|dx.
$$

---

## 5. Формула Тейлора

Формула:

$$
f(x)=\sum_{k=0}^{n}\frac{f^{(k)}(a)}{k!}(x-a)^k+R_n(x).
$$

Форма Лагранжа:

$$
R_n(x)=\frac{f^{(n+1)}(\xi)}{(n+1)!}(x-a)^{n+1}.
$$

Форма Пеано:

$$
R_n(x)=o((x-a)^n).
$$

Интегральная форма:

$$
R_n(x)=\frac{1}{n!}\int_a^x f^{(n+1)}(t)(x-t)^n dt.
$$

Форма Лагранжа удобна для оценок погрешности, форма Пеано - для локального анализа.

---

## 6. Экстремум

Теорема Ферма:

если функция дифференцируема в точке локального экстремума, то:

$$
f'(x_0)=0.
$$

Это необходимое, но не достаточное условие.

Достаточные условия:

- производная меняет знак с плюса на минус - максимум;
- с минуса на плюс - минимум;
- если `f''(x0)>0`, то минимум;
- если `f''(x0)<0`, то максимум.

---

## 7. Числовые ряды

Ряд:

$$
\sum a_n
$$

сходится, если сходится последовательность частичных сумм.

Абсолютная сходимость:

$$
\sum |a_n|
$$

сходится.

Условная сходимость: ряд сходится, но не абсолютно.

Интегральный признак: если `f` положительна, непрерывна и убывает, то ряд `sum f(n)` и интеграл от `f` сходятся или расходятся одновременно.

---

## 8. Функциональные и степенные ряды

Функциональный ряд:

$$
\sum u_n(x).
$$

Равномерная сходимость важна, потому что позволяет переносить свойства с членов ряда на сумму.

Если `u_n` непрерывны и ряд сходится равномерно, сумма непрерывна.

Степенной ряд:

$$
\sum c_n(x-a)^n.
$$

Радиус сходимости по Коши-Адамару:

$$
R=\frac{1}{\limsup\sqrt[n]{|c_n|}}.
$$

Внутри радиуса ряд сходится абсолютно, вне - расходится, на границе нужно проверять отдельно.

---

## 9. Ряды Фурье

Ряд Фурье:

$$
\frac{a_0}{2}+\sum_{n=1}^{\infty}(a_n\cos nx+b_n\sin nx).
$$

Коэффициенты:

$$
a_n=\frac{1}{\pi}\int_{-\pi}^{\pi}f(x)\cos nx dx.
$$

$$
b_n=\frac{1}{\pi}\int_{-\pi}^{\pi}f(x)\sin nx dx.
$$

Теорема Римана-Лебега:

```text
коэффициенты Фурье интегрируемой функции стремятся к нулю.
```

Теорема Фейера: средние арифметические частичных сумм ряда Фурье непрерывной периодической функции сходятся к функции равномерно.

Теоремы Вейерштрасса: непрерывные функции можно равномерно приближать алгебраическими или тригонометрическими многочленами.

---

## 10. Пространство L2

Пространство `L2` состоит из функций с интегрируемым квадратом.

Скалярное произведение:

$$
(f,g)=\int_a^b f(x)g(x)dx.
$$

Норма:

$$
\|f\|=\sqrt{(f,f)}.
$$

Ортонормированная система:

```text
разные элементы ортогональны, норма каждого равна 1.
```

Неравенство Бесселя:

$$
\sum |(f,e_n)|^2\le \|f\|^2.
$$

Равенство Парсеваля:

$$
\sum |(f,e_n)|^2= \|f\|^2.
$$

Парсеваль выполняется для полной ортонормированной системы.

---

## 11. Полный устный ответ

Математический анализ изучает предельные процессы. Непрерывность можно определять через предел, epsilon-delta или последовательности. На компакте непрерывная функция ограничена и достигает экстремумов, а по теореме Кантора равномерно непрерывна. Производная описывает локальную скорость изменения, а теорема Лагранжа связывает среднюю и мгновенную скорость. Интеграл Римана определяется через интегральные суммы, а формула Ньютона-Лейбница связывает интеграл с первообразной. Формула Тейлора приближает функцию многочленом с остаточным членом. Ряды изучаются через сходимость, абсолютную и условную сходимость, равномерную сходимость, степенные ряды и радиус Коши-Адамара. Ряды Фурье разлагают функции по тригонометрической системе, а пространство `L2` задает естественную среду для ортонормированных систем, неравенства Бесселя и равенства Парсеваля.

