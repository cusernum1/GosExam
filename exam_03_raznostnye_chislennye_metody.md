# ГЭ 3. Разностные численные методы

Файл подготовлен как отдельный экзаменационный конспект по численным методам, интерполяции, квадратурам, МНК, ОДУ и УЧП.

---

## 1. Численные методы линейной алгебры

Численные методы линейной алгебры применяются для решения систем линейных уравнений, вычисления собственных значений, обращения матриц и решения задач аппроксимации.

Основная задача:

$$
Ax=b.
$$

Методы делятся на прямые и итерационные.

Прямые методы за конечное число арифметических операций дают решение в точной арифметике:

- метод Гаусса;
- LU-разложение;
- метод Холецкого для симметричных положительно определенных матриц.

Итерационные методы строят последовательность приближений:

- метод простой итерации;
- метод Якоби;
- метод Зейделя;
- метод релаксации;
- метод сопряженных градиентов.

Важные понятия:

- обусловленность матрицы;
- устойчивость метода;
- норма невязки;
- сходимость итераций.

---

## 2. Интерполяция и многочлен Лагранжа

Интерполяция - построение функции, проходящей через заданные точки.

Пусть заданы узлы:

$$
x_0,x_1,\dots,x_n
$$

и значения:

$$
f(x_0),f(x_1),\dots,f(x_n).
$$

Интерполяционный многочлен Лагранжа:

$$
L_n(x)=\sum_{k=0}^{n}f(x_k)l_k(x),
$$

где базисные многочлены:

$$
l_k(x)=\prod_{j=0,j\ne k}^{n}\frac{x-x_j}{x_k-x_j}.
$$

Свойство:

$$
l_k(x_i)=0 \text{ при } i\ne k,
$$

и:

$$
l_k(x_k)=1.
$$

Поэтому:

$$
L_n(x_i)=f(x_i).
$$

---

## 3. Оценка погрешности интерполяции

Если функция имеет непрерывную производную порядка `n+1`, то:

$$
f(x)-L_n(x)=\frac{f^{(n+1)}(\xi)}{(n+1)!}
\prod_{k=0}^{n}(x-x_k),
$$

где `xi` лежит между узлами и точкой `x`.

Отсюда оценка:

$$
|f(x)-L_n(x)|\le
\frac{M_{n+1}}{(n+1)!}
\left|\prod_{k=0}^{n}(x-x_k)\right|,
$$

где:

$$
M_{n+1}=\max |f^{(n+1)}(x)|.
$$

---

## 4. Интерполяция и приближение функций одного переменного

Интерполяция требует точного совпадения с функцией в узлах.

Аппроксимация допускает отклонение, но стремится сделать его малым по выбранному критерию.

Основные методы:

- интерполяционные многочлены;
- сплайны;
- тригонометрическая интерполяция;
- метод наименьших квадратов;
- равномерное приближение;
- приближение ортогональными многочленами.

---

## 5. Метод наименьших квадратов

Метод наименьших квадратов строит приближающую функцию так, чтобы сумма квадратов отклонений была минимальной.

Пусть:

$$
y_i\approx a_0+a_1x_i+\dots+a_mx_i^m.
$$

Функционал:

$$
S(a_0,\dots,a_m)=\sum_{i=1}^{n}(y_i-P_m(x_i))^2.
$$

Необходимые условия минимума:

$$
\frac{\partial S}{\partial a_j}=0.
$$

Они приводят к нормальной системе линейных уравнений для коэффициентов.

### Линеаризация

Если модель нелинейна по параметрам, иногда ее можно линеаризовать.

Пример:

```text
y = ae^{bx}
```

Логарифмируем:

```text
ln y = ln a + bx
```

После этого можно применить линейный МНК.

---

## 6. Численное интегрирование

Численное интегрирование используется, когда первообразная неизвестна или вычисляется сложно.

Нужно приближенно найти:

$$
I=\int_a^b f(x)dx.
$$

Квадратурная формула:

$$
I\approx \sum_{k=0}^{n}A_k f(x_k).
$$

Здесь `x_k` - узлы, `A_k` - веса.

---

## 7. Формулы прямоугольников и остаточные члены

Левые прямоугольники:

$$
I\approx h\sum_{k=0}^{n-1}f(x_k).
$$

Правые прямоугольники:

$$
I\approx h\sum_{k=1}^{n}f(x_k).
$$

Средние прямоугольники:

$$
I\approx h\sum_{k=0}^{n-1}f\left(\frac{x_k+x_{k+1}}{2}\right).
$$

Для формулы средних прямоугольников погрешность составной формулы имеет порядок:

```text
O(h^2)
```

Для левых и правых прямоугольников обычно порядок:

```text
O(h)
```

---

## 8. Формула Симпсона и правило Рунге

Формула Симпсона на одном отрезке:

$$
\int_a^b f(x)dx\approx
\frac{b-a}{6}\left(f(a)+4f\left(\frac{a+b}{2}\right)+f(b)\right).
$$

Составная формула Симпсона требует четного числа промежутков:

$$
I\approx \frac{h}{3}
\left(f_0+f_n+4\sum f_{2k-1}+2\sum f_{2k}\right).
$$

Порядок погрешности составной формулы Симпсона:

```text
O(h^4)
```

Правило Рунге применяется для оценки погрешности:

$$
R\approx \frac{I_h-I_{h/2}}{2^p-1},
$$

где `p` - порядок метода.

---

## 9. Квадратурные формулы наивысшей алгебраической степени точности

Алгебраическая степень точности квадратурной формулы - наибольшая степень многочлена, для которого формула точна.

Формулы Гаусса имеют максимальную степень точности при заданном числе узлов.

Если узлов `n`, то формула Гаусса точна для многочленов степени до:

$$
2n-1.
$$

Узлы выбираются как корни ортогонального многочлена.

---

## 10. Многочлены Лежандра

Многочлены Лежандра ортогональны на отрезке `[-1,1]` с весом `1`.

Первые многочлены:

$$
P_0(x)=1.
$$

$$
P_1(x)=x.
$$

$$
P_2(x)=\frac{1}{2}(3x^2-1).
$$

Свойство ортогональности:

$$
\int_{-1}^{1}P_m(x)P_n(x)dx=0
$$

при `m` не равно `n`.

Корни многочлена Лежандра используются как узлы формулы Гаусса на `[-1,1]`.

---

## 11. Формула Гаусса для интегралов

Формула Гаусса:

$$
\int_{-1}^{1}f(x)dx\approx \sum_{k=1}^{n}A_k f(x_k),
$$

где `x_k` - корни многочлена Лежандра `P_n(x)`.

Для произвольного отрезка `[a,b]` используется замена:

$$
x=\frac{a+b}{2}+\frac{b-a}{2}t.
$$

Тогда:

$$
\int_a^b f(x)dx=
\frac{b-a}{2}\int_{-1}^{1}
f\left(\frac{a+b}{2}+\frac{b-a}{2}t\right)dt.
$$

---

## 12. Несобственные интегралы

Несобственные интегралы возникают:

- на бесконечных промежутках;
- при неограниченной функции;
- при особенностях внутри промежутка.

Приемы вычисления:

1. Переход к пределу.
2. Замена переменной.
3. Выделение особенности.
4. Разбиение интеграла.
5. Сравнение с эталонными интегралами.
6. Численное интегрирование после преобразования.

Пример:

$$
\int_a^{+\infty}f(x)dx=
\lim_{R\to+\infty}\int_a^R f(x)dx.
$$

---

## 13. Численное решение ОДУ

Задача Коши:

$$
y'=f(x,y), \qquad y(x_0)=y_0.
$$

Метод Эйлера:

$$
y_{k+1}=y_k+h f(x_k,y_k).
$$

Локальная погрешность метода Эйлера имеет порядок `O(h^2)`.

Глобальная погрешность имеет порядок `O(h)`.

Методы Рунге-Кутты строят более точные одношаговые методы. Классический метод четвертого порядка имеет глобальную погрешность `O(h^4)`.

---

## 14. Численное решение УЧП

Для уравнений в частных производных часто применяют разностные схемы.

Идея:

- заменить область сеткой;
- заменить производные конечными разностями;
- получить систему алгебраических уравнений.

Для теплопроводности:

$$
u_t=a^2u_{xx}
$$

явная схема:

$$
\frac{u_i^{j+1}-u_i^j}{\tau}
=a^2\frac{u_{i+1}^j-2u_i^j+u_{i-1}^j}{h^2}.
$$

Важные свойства схем:

- аппроксимация;
- устойчивость;
- сходимость.

---

## 15. Экзаменационный минимум

Нужно знать:

1. Прямые и итерационные методы линейной алгебры.
2. Многочлен Лагранжа и его погрешность.
3. МНК и нормальные уравнения.
4. Формулы прямоугольников, Симпсона, Гаусса.
5. Правило Рунге.
6. Многочлены Лежандра.
7. Приемы для несобственных интегралов.
8. Метод Эйлера и Рунге-Кутты.
9. Основы разностных схем для УЧП.

