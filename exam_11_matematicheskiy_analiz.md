# ГЭ 11. Математический анализ

Файл подготовлен как отдельный экзаменационный конспект по ключевым теоретическим вопросам математического анализа.

---

## 1. Непрерывная функция

Функция `f` непрерывна в точке `a`, если:

$$
\lim_{x\to a}f(x)=f(a).
$$

Эквивалентное определение на языке `epsilon-delta`:

для любого `epsilon > 0` существует `delta > 0` такое, что из `|x-a|<delta` следует:

$$
|f(x)-f(a)|<\varepsilon.
$$

Определение через последовательности:

если `x_n -> a`, то:

$$
f(x_n)\to f(a).
$$

Компакт в `R` - замкнутое и ограниченное множество. Непрерывный образ компакта является компактом. Следовательно, непрерывная функция на компакте ограничена и достигает максимума и минимума.

---

## 2. Равномерная непрерывность. Теорема Кантора

Функция равномерно непрерывна на множестве `E`, если:

для любого `epsilon > 0` существует `delta > 0`, одно и то же для всех точек множества, такое, что из `|x-y|<delta` следует:

$$
|f(x)-f(y)|<\varepsilon.
$$

Теорема Кантора:

```text
если функция непрерывна на компакте, то она равномерно непрерывна на этом компакте.
```

Отличие от обычной непрерывности: при равномерной непрерывности `delta` зависит только от `epsilon`, но не зависит от точки.

---

## 3. Производная и теорема Лагранжа

Производная функции в точке:

$$
f'(x_0)=\lim_{\Delta x\to0}
\frac{f(x_0+\Delta x)-f(x_0)}{\Delta x}.
$$

Геометрический смысл: производная равна угловому коэффициенту касательной к графику функции.

Теорема Лагранжа:

если функция непрерывна на `[a,b]` и дифференцируема на `(a,b)`, то существует `c` из `(a,b)` такое, что:

$$
f'(c)=\frac{f(b)-f(a)}{b-a}.
$$

---

## 4. Интеграл Римана и формула Ньютона-Лейбница

Интеграл Римана определяется как предел интегральных сумм при стремлении диаметра разбиения к нулю.

Если `f` непрерывна на `[a,b]`, то она интегрируема по Риману.

Формула Ньютона-Лейбница:

если `F'(x)=f(x)`, то:

$$
\int_a^b f(x)dx=F(b)-F(a).
$$

---

## 5. Интегрируемость модуля

Если функция `f` интегрируема по Риману на `[a,b]`, то функция `|f|` также интегрируема на `[a,b]`.

Кроме того:

$$
\left|\int_a^b f(x)dx\right|
\le
\int_a^b |f(x)|dx.
$$

Эта теорема важна для оценок интегралов и признаков сходимости.

---

## 6. Формула Тейлора

Формула Тейлора в точке `a`:

$$
f(x)=f(a)+f'(a)(x-a)+\dots+
\frac{f^{(n)}(a)}{n!}(x-a)^n+R_n(x).
$$

Остаточный член в форме Лагранжа:

$$
R_n(x)=\frac{f^{(n+1)}(\xi)}{(n+1)!}(x-a)^{n+1}.
$$

Остаточный член в форме Пеано:

$$
R_n(x)=o((x-a)^n).
$$

Остаточный член в интегральной форме:

$$
R_n(x)=\frac{1}{n!}\int_a^x f^{(n+1)}(t)(x-t)^n dt.
$$

---

## 7. Экстремум. Теорема Ферма

Теорема Ферма:

если функция имеет локальный экстремум в точке `x0` и дифференцируема в этой точке, то:

$$
f'(x_0)=0.
$$

Это необходимое условие экстремума.

Достаточные условия:

- если производная меняет знак с плюса на минус, то максимум;
- если с минуса на плюс, то минимум;
- если `f''(x0)>0`, то минимум;
- если `f''(x0)<0`, то максимум.

---

## 8. Числовые ряды

Числовой ряд:

$$
\sum_{n=1}^{\infty}a_n.
$$

Ряд сходится, если существует конечный предел частичных сумм.

Абсолютная сходимость:

$$
\sum |a_n|
$$

сходится.

Условная сходимость: ряд сходится, но не сходится абсолютно.

Интегральный признак:

если `f(x)` положительна, непрерывна и убывает, а `a_n=f(n)`, то ряд и интеграл:

$$
\sum_{n=1}^{\infty}a_n,
\qquad
\int_1^{\infty}f(x)dx
$$

сходятся или расходятся одновременно.

---

## 9. Функциональные ряды

Функциональный ряд:

$$
\sum_{n=1}^{\infty}u_n(x).
$$

Он сходится равномерно на множестве `E`, если остаток ряда стремится к нулю равномерно по `x`.

Если функции `u_n(x)` непрерывны и ряд сходится равномерно, то сумма ряда непрерывна.

Это одно из главных свойств равномерной сходимости: она позволяет переносить непрерывность на сумму.

---

## 10. Степенные ряды

Степенной ряд:

$$
\sum_{n=0}^{\infty}c_n(x-a)^n.
$$

Теорема Коши-Адамара:

радиус сходимости:

$$
R=\frac{1}{\limsup\sqrt[n]{|c_n|}}.
$$

Внутри интервала `|x-a|<R` ряд сходится абсолютно.

Вне интервала `|x-a|>R` ряд расходится.

На концах интервала вопрос решается отдельно.

---

## 11. Ряд Фурье и интеграл Фурье

Тригонометрический ряд Фурье функции:

$$
\frac{a_0}{2}+\sum_{n=1}^{\infty}(a_n\cos nx+b_n\sin nx).
$$

Коэффициенты Фурье:

$$
a_n=\frac{1}{\pi}\int_{-\pi}^{\pi}f(x)\cos nx dx.
$$

$$
b_n=\frac{1}{\pi}\int_{-\pi}^{\pi}f(x)\sin nx dx.
$$

Теорема Римана-Лебега:

```text
коэффициенты Фурье интегрируемой функции стремятся к нулю.
```

Интеграл Фурье является аналогом ряда Фурье для функций на бесконечном промежутке.

---

## 12. Теорема Фейера и теоремы Вейерштрасса

Теорема Фейера утверждает, что для непрерывной периодической функции средние арифметические частичных сумм ее ряда Фурье равномерно сходятся к самой функции.

Идея:

```text
даже если сам ряд Фурье ведет себя сложно, его средние Фейера обладают хорошей сходимостью.
```

Теоремы Вейерштрасса о приближении:

1. Непрерывную функцию на отрезке можно равномерно приблизить алгебраическими многочленами.
2. Непрерывную периодическую функцию можно равномерно приблизить тригонометрическими многочленами.

---

## 13. Пространство L2 и ортонормированные системы

Пространство `L2` состоит из функций, квадрат которых интегрируем.

Скалярное произведение:

$$
(f,g)=\int_a^b f(x)g(x)dx.
$$

Норма:

$$
\|f\|=\sqrt{(f,f)}.
$$

Ортонормированная система:

$$
(e_i,e_j)=0 \text{ при } i\ne j,
$$

и:

$$
\|e_i\|=1.
$$

Неравенство Бесселя:

$$
\sum |(f,e_n)|^2\le \|f\|^2.
$$

Равенство Парсеваля для полной ортонормированной системы:

$$
\sum |(f,e_n)|^2= \|f\|^2.
$$

Система называется полной, если нельзя добавить ненулевую функцию, ортогональную всем элементам системы.

Система называется замкнутой, если равенство Парсеваля выполняется для всех функций пространства.

---

## 14. Экзаменационный минимум

Нужно знать:

1. Определения непрерывности.
2. Компакт и непрерывный образ компакта.
3. Теорему Кантора.
4. Производную и теорему Лагранжа.
5. Интеграл Римана и формулу Ньютона-Лейбница.
6. Формулу Тейлора и остаточные члены.
7. Теорему Ферма.
8. Абсолютную и условную сходимость рядов.
9. Равномерную сходимость функциональных рядов.
10. Теорему Коши-Адамара.
11. Ряды Фурье, Римана-Лебега, Фейера.
12. Пространство `L2`, Бесселя и Парсеваля.

