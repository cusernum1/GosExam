# Конспект по типовым задачам математического анализа для госэкзамена

Этот файл сделан как учебный конспект: не только формулы, но и объяснение, когда они применяются, что означает каждый элемент формулы и как пошагово решать задачи.

Конспект ориентирован на учебную литературу, указанную в программе:

Основная электронная учебно-методическая документация:

1. Кудрявцев Л.Д. Краткий курс математического анализа. Т. 1. Дифференциальное и интегральное исчисления функций одной переменной. Ряды. М.: Физматлит, 2008. 400 с. URL: http://e.lanbook.com/book/2224.
2. Кудрявцев Л.Д. Краткий курс математического анализа. Т. 2. Дифференциальное и интегральное исчисления функций многих переменных. Гармонический анализ. М.: Физматлит, 2003. 424 с. URL: http://e.lanbook.com/book/2225.

Дополнительная электронная учебно-методическая документация:

3. Запорожец Г.И. Руководство к решению задач по математическому анализу. СПб.: Лань, 2014. 464 с. URL: http://e.lanbook.com/book/149.

Основная печатная учебно-методическая документация:

4. Фихтенгольц Г.М. Курс дифференциального и интегрального исчисления. Т. 1. 8-е изд. М.: Физматлит, 2006. 679 с.
5. Фихтенгольц Г.М. Курс дифференциального и интегрального исчисления. Т. 2. 8-е изд. М.; СПб.: Физматлит; Наука, 2001. 863 с.
6. Фихтенгольц Г.М. Курс дифференциального и интегрального исчисления. Т. 3. 8-е изд. М.; СПб.: Физматлит; Невский диалект, 2002. 727 с.

Дополнительная печатная учебно-методическая документация:

7. Карачик В.В. Курс математического анализа: учебное пособие для вузов по инженерно-физическим и физико-математическим специальностям. Челябинск: Издательский Центр ЮУрГУ, 2009. 681 с.

Материал ниже изложен своими словами в учебном формате подготовки к госэкзамену: определения, формулы, условия применимости, алгоритмы решения и подробные примеры.

Разделы:

1. Разложение функций в ряд Тейлора и Маклорена.
2. Разложение по степеням выражения, например по степеням `x+2`.
3. Вычисление длины дуги кривой.
4. Вычисление объема тела вращения.
5. Суммы числовых рядов.
6. Площадь фигур в полярных координатах.
7. Приближенное вычисление интегралов.

---

## 1. Ряд Тейлора и ряд Маклорена

### 1.1. Основная идея

Ряд Тейлора позволяет представить функцию в виде бесконечного степенного ряда около некоторой точки.

Идея такая: если функция достаточно гладкая, то около точки `a` ее можно приближать многочленами:

```text
f(x) приблизительно равно многочлену по степеням (x-a).
```

Чем больше членов ряда берется, тем точнее приближение, если ряд действительно сходится к функции.

### 1.2. Формула ряда Тейлора

Пусть функция `f(x)` имеет производные всех порядков в окрестности точки `a`. Тогда формальный ряд Тейлора имеет вид:

$$
f(x)=\sum_{n=0}^{\infty}\frac{f^{(n)}(a)}{n!}(x-a)^n.
$$

Здесь:

- `a` - центр разложения;
- `f^{(n)}(a)` - производная `n`-го порядка в точке `a`;
- `n!` - факториал;
- `(x-a)^n` - степени отклонения от центра.

Первые члены ряда:

$$
f(x)=f(a)+f'(a)(x-a)+\frac{f''(a)}{2!}(x-a)^2+
\frac{f'''(a)}{3!}(x-a)^3+\dots
$$

### 1.3. Ряд Маклорена

Ряд Маклорена - это частный случай ряда Тейлора при `a=0`.

Формула:

$$
f(x)=\sum_{n=0}^{\infty}\frac{f^{(n)}(0)}{n!}x^n.
$$

То есть разложение идет по степеням `x`.

### 1.4. Область сходимости

Когда функцию раскладывают в степенной ряд, обязательно нужно указать область сходимости.

Степенной ряд обычно имеет вид:

$$
\sum_{n=0}^{\infty}c_n(x-a)^n.
$$

Для него существует радиус сходимости `R`, такой что:

- при `|x-a|<R` ряд сходится;
- при `|x-a|>R` ряд расходится;
- при `|x-a|=R` концы нужно проверять отдельно.

Основные способы найти радиус сходимости:

1. По признаку Даламбера:

$$
\lim_{n\to\infty}\left|\frac{u_{n+1}}{u_n}\right|<1.
$$

2. По признаку Коши:

$$
\lim_{n\to\infty}\sqrt[n]{|u_n|}<1.
$$

Здесь `u_n` - общий член ряда.

### 1.5. Стандартные разложения

Эти ряды нужно хорошо знать, потому что большинство задач решается через них.

#### Экспонента

$$
e^x=\sum_{n=0}^{\infty}\frac{x^n}{n!}
=1+x+\frac{x^2}{2!}+\frac{x^3}{3!}+\dots
$$

Область сходимости:

$$
x\in\mathbb R.
$$

#### Синус

$$
\sin x=\sum_{n=0}^{\infty}(-1)^n\frac{x^{2n+1}}{(2n+1)!}
=x-\frac{x^3}{3!}+\frac{x^5}{5!}-\dots
$$

Область сходимости:

$$
x\in\mathbb R.
$$

#### Косинус

$$
\cos x=\sum_{n=0}^{\infty}(-1)^n\frac{x^{2n}}{(2n)!}
=1-\frac{x^2}{2!}+\frac{x^4}{4!}-\dots
$$

Область сходимости:

$$
x\in\mathbb R.
$$

#### Геометрический ряд

$$
\frac{1}{1-x}=\sum_{n=0}^{\infty}x^n
=1+x+x^2+x^3+\dots
$$

Область сходимости:

$$
|x|<1.
$$

#### Логарифм

$$
\ln(1+x)=\sum_{n=1}^{\infty}(-1)^{n-1}\frac{x^n}{n}
=x-\frac{x^2}{2}+\frac{x^3}{3}-\dots
$$

Область сходимости:

$$
-1<x\le 1.
$$

Также:

$$
\ln(1-x)=-\sum_{n=1}^{\infty}\frac{x^n}{n}
=-x-\frac{x^2}{2}-\frac{x^3}{3}-\dots
$$

Область сходимости:

$$
-1\le x<1.
$$

#### Арктангенс

$$
\arctan x=\sum_{n=0}^{\infty}(-1)^n\frac{x^{2n+1}}{2n+1}
=x-\frac{x^3}{3}+\frac{x^5}{5}-\dots
$$

Область сходимости:

$$
|x|\le 1.
$$

### 1.6. Как раскладывать функцию в ряд

Общий алгоритм:

1. Определить центр разложения.
2. Если центр не равен нулю, ввести замену `t=x-a`.
3. Переписать функцию через `t`.
4. Использовать стандартные ряды.
5. Упростить выражение.
6. Найти область сходимости.
7. Проверить концы интервала, если они возникают.

---

## 2. Разложение по степеням `x+2`

### 2.1. Что означает "по степеням `x+2`"

Если просят разложить функцию по степеням `x+2`, это означает, что ряд должен иметь вид:

$$
c_0+c_1(x+2)+c_2(x+2)^2+c_3(x+2)^3+\dots
$$

Такой ряд является рядом Тейлора в точке:

$$
x_0=-2.
$$

Потому что:

$$
x-x_0=x-(-2)=x+2.
$$

### 2.2. Пример: разложить `sin 5x` по степеням `x+2`

Дано:

$$
f(x)=\sin 5x.
$$

Нужно получить ряд по степеням `x+2`.

Вводим замену:

$$
t=x+2.
$$

Тогда:

$$
x=t-2.
$$

Подставляем в функцию:

$$
\sin 5x=\sin(5(t-2))=\sin(5t-10).
$$

Теперь используем формулу синуса разности:

$$
\sin(\alpha-\beta)=\sin\alpha\cos\beta-\cos\alpha\sin\beta.
$$

В нашем случае:

$$
\alpha=5t,\qquad \beta=10.
$$

Поэтому:

$$
\sin(5t-10)=\sin 5t\cos 10-\cos 5t\sin 10.
$$

Теперь раскладываем `sin 5t` и `cos 5t`.

Для синуса:

$$
\sin 5t=\sum_{n=0}^{\infty}(-1)^n\frac{(5t)^{2n+1}}{(2n+1)!}.
$$

Для косинуса:

$$
\cos 5t=\sum_{n=0}^{\infty}(-1)^n\frac{(5t)^{2n}}{(2n)!}.
$$

Подставляем:

$$
\sin 5x=
\cos 10\sum_{n=0}^{\infty}(-1)^n
\frac{5^{2n+1}(x+2)^{2n+1}}{(2n+1)!}
-
\sin 10\sum_{n=0}^{\infty}(-1)^n
\frac{5^{2n}(x+2)^{2n}}{(2n)!}.
$$

Первые члены:

$$
\sin 5x=
-\sin 10
+5\cos 10(x+2)
+\frac{25}{2}\sin 10(x+2)^2
-\frac{125}{6}\cos 10(x+2)^3
-\frac{625}{24}\sin 10(x+2)^4+\dots
$$

Область сходимости:

$$
x\in\mathbb R.
$$

Почему: ряды синуса и косинуса сходятся при всех действительных значениях аргумента.

---

## 3. Ряд Маклорена для `ln(1-x^2)`

Дано:

$$
f(x)=\ln(1-x^2).
$$

Нужно разложить в ряд Маклорена, то есть по степеням `x`.

Используем стандартный ряд:

$$
\ln(1-t)=-\sum_{n=1}^{\infty}\frac{t^n}{n},
\qquad |t|<1.
$$

В нашей функции вместо `t` стоит:

$$
t=x^2.
$$

Подставляем:

$$
\ln(1-x^2)=-\sum_{n=1}^{\infty}\frac{(x^2)^n}{n}.
$$

Так как:

$$
(x^2)^n=x^{2n},
$$

получаем:

$$
\ln(1-x^2)=-\sum_{n=1}^{\infty}\frac{x^{2n}}{n}.
$$

То есть:

$$
\ln(1-x^2)
=-x^2-\frac{x^4}{2}-\frac{x^6}{3}-\frac{x^8}{4}-\dots
$$

Область сходимости:

$$
|x^2|<1.
$$

Так как `x^2>=0`, условие равносильно:

$$
|x|<1.
$$

В точках `x=1` и `x=-1` сама функция `ln(1-x^2)` не определена, потому что получается `ln 0`. Поэтому концы не включаются.

Ответ:

$$
\ln(1-x^2)=-\sum_{n=1}^{\infty}\frac{x^{2n}}{n},
\qquad x\in(-1,1).
$$

---

## 4. Длина дуги кривой

### 4.1. Геометрический смысл

Длина дуги - это длина участка кривой. Если кривая задана графиком функции, параметрически или в полярных координатах, формулы будут разными, но смысл один:

```text
маленький участок кривой заменяется маленьким отрезком.
```

Длина маленького отрезка находится по теореме Пифагора.

### 4.2. Если кривая задана явно: `y=f(x)`

Пусть кривая задана:

$$
y=f(x),\qquad a\le x\le b.
$$

Тогда длина дуги:

$$
L=\int_a^b\sqrt{1+(f'(x))^2}\,dx.
$$

Объяснение формулы:

Малое приращение по горизонтали равно `dx`.

Малое приращение по вертикали:

$$
dy=f'(x)dx.
$$

Тогда элемент длины:

$$
ds=\sqrt{dx^2+dy^2}
=\sqrt{dx^2+(f'(x)dx)^2}
=\sqrt{1+(f'(x))^2}\,dx.
$$

Интегрируя `ds`, получаем длину всей дуги.

### 4.3. Если кривая задана параметрически

Пусть:

$$
x=x(t),\qquad y=y(t),\qquad \alpha\le t\le\beta.
$$

Тогда:

$$
L=\int_\alpha^\beta\sqrt{(x'(t))^2+(y'(t))^2}\,dt.
$$

Объяснение:

$$
dx=x'(t)dt,\qquad dy=y'(t)dt.
$$

По теореме Пифагора:

$$
ds=\sqrt{dx^2+dy^2}
=\sqrt{(x'(t))^2+(y'(t))^2}\,dt.
$$

### 4.4. Если кривая задана в полярных координатах

Пусть:

$$
\rho=\rho(\varphi),\qquad \alpha\le\varphi\le\beta.
$$

Тогда длина дуги:

$$
L=\int_\alpha^\beta\sqrt{\rho^2+\left(\frac{d\rho}{d\varphi}\right)^2}\,d\varphi.
$$

Эта формула получается из связи полярных и декартовых координат:

$$
x=\rho\cos\varphi,\qquad y=\rho\sin\varphi.
$$

### 4.5. Алгоритм решения задач на длину дуги

1. Определить, как задана кривая: явно, параметрически или полярно.
2. Записать соответствующую формулу длины.
3. Найти нужные производные.
4. Подставить производные в формулу.
5. Упростить подкоренное выражение.
6. Подставить пределы интегрирования.
7. Вычислить интеграл.

### 4.6. Пример: параметрическая кривая

Дано:

$$
x=3(2t-\sin 2t),\qquad y=3(1-\cos 2t).
$$

Формула длины:

$$
L=\int_a^b\sqrt{(x'(t))^2+(y'(t))^2}\,dt.
$$

Находим производные.

Для `x(t)`:

$$
x'(t)=3(2-2\cos 2t)=6(1-\cos 2t).
$$

Для `y(t)`:

$$
y'(t)=3\cdot 2\sin 2t=6\sin 2t.
$$

Используем формулы:

$$
1-\cos 2t=2\sin^2 t,
$$

$$
\sin 2t=2\sin t\cos t.
$$

Тогда:

$$
x'(t)=12\sin^2 t,
$$

$$
y'(t)=12\sin t\cos t.
$$

Подставляем:

$$
\sqrt{(x')^2+(y')^2}
=\sqrt{144\sin^4 t+144\sin^2t\cos^2t}.
$$

Выносим общий множитель:

$$
=12\sqrt{\sin^2t(\sin^2t+\cos^2t)}.
$$

Так как:

$$
\sin^2t+\cos^2t=1,
$$

получаем:

$$
\sqrt{(x')^2+(y')^2}=12|\sin t|.
$$

Если рассматривается одна арка циклоиды, обычно берут:

$$
0\le t\le\pi.
$$

На этом промежутке:

$$
\sin t\ge 0,
$$

поэтому:

$$
|\sin t|=\sin t.
$$

Тогда:

$$
L=\int_0^\pi12\sin t\,dt.
$$

Вычисляем:

$$
L=12[-\cos t]_0^\pi
=12(-\cos\pi+\cos0)
=12(1+1)=24.
$$

Ответ:

$$
L=24.
$$

Важно: если в задаче не указан промежуток изменения параметра, нужно уточнить его. Без промежутка длина дуги не определяется однозначно.

---

## 5. Объем тела вращения

### 5.1. Основная идея

Если плоскую фигуру вращать вокруг оси, получается тело вращения. Его объем часто вычисляют через интеграл.

Наиболее важные методы:

1. Метод дисков.
2. Метод шайб.
3. Метод цилиндрических слоев.

### 5.2. Метод дисков

Если область под графиком `y=f(x)` на отрезке `[a,b]` вращается вокруг оси `Ox`, то сечение перпендикулярно оси `Ox` является кругом радиуса `f(x)`.

Площадь круга:

$$
S(x)=\pi R^2.
$$

Здесь:

$$
R=f(x).
$$

Поэтому объем:

$$
V=\pi\int_a^b(f(x))^2\,dx.
$$

### 5.3. Метод шайб

Если вращается область между двумя графиками:

$$
y=f(x),\qquad y=g(x),
$$

и `f(x)` выше `g(x)`, то при вращении вокруг `Ox` получается сечение в виде шайбы.

Внешний радиус:

$$
R=f(x).
$$

Внутренний радиус:

$$
r=g(x).
$$

Площадь шайбы:

$$
S(x)=\pi R^2-\pi r^2=\pi(R^2-r^2).
$$

Объем:

$$
V=\pi\int_a^b(R^2-r^2)\,dx.
$$

### 5.4. Вращение вокруг оси `Oy`

Если область между функциями выражается через `y`, удобнее использовать:

$$
V=\pi\int_c^d(R^2-r^2)\,dy.
$$

Здесь радиусы измеряются горизонтально, то есть как расстояния до оси `Oy`.

Если область задана через `x`, иногда удобнее использовать метод цилиндрических слоев.

### 5.5. Метод цилиндрических слоев

Если область под графиком `y=f(x)` вращается вокруг оси `Oy`, то:

$$
V=2\pi\int_a^b x f(x)\,dx.
$$

Здесь:

- `x` - радиус цилиндрического слоя;
- `f(x)` - высота слоя;
- `2\pi x` - длина окружности;
- `dx` - толщина слоя.

Для области между двумя функциями:

$$
V=2\pi\int_a^b x(f(x)-g(x))\,dx.
$$

### 5.6. Алгоритм решения задач на объем вращения

1. Нарисовать или мысленно представить область.
2. Определить ось вращения.
3. Понять, какой метод удобнее: диски, шайбы или цилиндрические слои.
4. Определить границы интегрирования.
5. Найти внешний и внутренний радиусы.
6. Составить интеграл.
7. Вычислить интеграл.

### 5.7. Пример: вращение вокруг оси абсцисс

Дано:

$$
y=2x+1,\qquad y=x+4,\qquad x=0,\qquad x=1.
$$

Фигура ограничена двумя прямыми и вертикальными линиями.

Сначала определим, какая прямая выше:

$$
(x+4)-(2x+1)=3-x.
$$

На отрезке `[0,1]`:

$$
3-x>0.
$$

Значит:

$$
y=x+4
$$

является верхней функцией, а

$$
y=2x+1
$$

является нижней функцией.

При вращении вокруг оси `Ox` используем метод шайб.

Внешний радиус:

$$
R=x+4.
$$

Внутренний радиус:

$$
r=2x+1.
$$

Формула:

$$
V=\pi\int_0^1(R^2-r^2)\,dx.
$$

Подставляем:

$$
V=\pi\int_0^1\left((x+4)^2-(2x+1)^2\right)\,dx.
$$

Раскрываем скобки:

$$
(x+4)^2=x^2+8x+16,
$$

$$
(2x+1)^2=4x^2+4x+1.
$$

Вычитаем:

$$
(x+4)^2-(2x+1)^2
=x^2+8x+16-4x^2-4x-1
=-3x^2+4x+15.
$$

Получаем:

$$
V=\pi\int_0^1(-3x^2+4x+15)\,dx.
$$

Интегрируем:

$$
\int(-3x^2+4x+15)\,dx=-x^3+2x^2+15x.
$$

Подставляем пределы:

$$
V=\pi[-x^3+2x^2+15x]_0^1.
$$

$$
V=\pi((-1+2+15)-0)=16\pi.
$$

Ответ:

$$
V=16\pi.
$$

---

## 6. Суммы числовых рядов

### 6.1. Что такое числовой ряд

Числовой ряд имеет вид:

$$
\sum_{n=1}^{\infty}a_n.
$$

Его сумма определяется через частичные суммы:

$$
S_N=a_1+a_2+\dots+a_N.
$$

Если существует конечный предел:

$$
\lim_{N\to\infty}S_N=S,
$$

то ряд называется сходящимся, а число `S` называется суммой ряда.

Если такого конечного предела нет, ряд расходится.

### 6.2. Основные способы найти сумму ряда

1. Телескопическое сокращение.
2. Разложение на простые дроби.
3. Использование известных рядов.
4. Дифференцирование или интегрирование степенных рядов.
5. Использование интегральных представлений.

### 6.3. Разложение на простые дроби

Если общий член имеет рациональный вид, например:

$$
\frac{1}{(4n-1)(2n+1)},
$$

часто его раскладывают так:

$$
\frac{1}{(4n-1)(2n+1)}
=\frac{A}{4n-1}+\frac{B}{2n+1}.
$$

После этого коэффициенты `A` и `B` находятся сравнением коэффициентов.

### 6.4. Интегральное представление

В задачах на сумму ряда бывает полезна формула:

$$
\frac1\alpha=\int_0^1x^{\alpha-1}\,dx,\qquad \alpha>0.
$$

Она следует из вычисления:

$$
\int_0^1x^{\alpha-1}\,dx=
\left[\frac{x^\alpha}{\alpha}\right]_0^1
=\frac1\alpha.
$$

Эта формула позволяет заменить дроби интегралами и иногда суммировать ряд как геометрическую прогрессию.

### 6.5. Пример: найти сумму ряда

Дано:

$$
\sum_{n=1}^{\infty}\frac{1}{(4n-1)(2n+1)}.
$$

Разложим общий член на простые дроби:

$$
\frac{1}{(4n-1)(2n+1)}
=\frac{A}{4n-1}+\frac{B}{2n+1}.
$$

Умножаем обе части на `(4n-1)(2n+1)`:

$$
1=A(2n+1)+B(4n-1).
$$

Раскрываем:

$$
1=(2A+4B)n+(A-B).
$$

Сравниваем коэффициенты:

$$
2A+4B=0,
$$

$$
A-B=1.
$$

Из первой формулы:

$$
A=-2B.
$$

Подставляем во вторую:

$$
-2B-B=1.
$$

$$
-3B=1,\qquad B=-\frac13.
$$

Тогда:

$$
A=\frac23.
$$

Следовательно:

$$
\frac{1}{(4n-1)(2n+1)}
=\frac{2}{3(4n-1)}-\frac{1}{3(2n+1)}.
$$

Приводим к виду с `n`:

$$
4n-1=4\left(n-\frac14\right),
$$

$$
2n+1=2\left(n+\frac12\right).
$$

Получаем:

$$
\frac{1}{(4n-1)(2n+1)}
=\frac16\left(
\frac{1}{n-\frac14}
-\frac{1}{n+\frac12}
\right).
$$

Тогда сумма:

$$
S=\frac16\sum_{n=1}^{\infty}
\left(
\frac{1}{n-\frac14}
-\frac{1}{n+\frac12}
\right).
$$

Дальше применяем интегральное представление:

$$
\frac1\alpha=\int_0^1x^{\alpha-1}\,dx.
$$

Итоговое преобразование приводит к интегралу:

$$
S=\frac16\int_0^1
\frac{x^{-1/4}-x^{1/2}}{1-x}\,dx.
$$

Делаем замену:

$$
x=t^4,\qquad dx=4t^3dt.
$$

Тогда:

$$
x^{-1/4}=t^{-1},\qquad x^{1/2}=t^2.
$$

Получаем:

$$
S=\frac16\int_0^1
\frac{4t^2(1-t^3)}{1-t^4}\,dt.
$$

Раскладываем:

$$
1-t^3=(1-t)(1+t+t^2),
$$

$$
1-t^4=(1-t)(1+t)(1+t^2).
$$

Сокращаем общий множитель `(1-t)`:

$$
S=\frac16\int_0^1
\frac{4t^2(1+t+t^2)}{(1+t)(1+t^2)}\,dt.
$$

После разложения рациональной функции и интегрирования:

$$
\int_0^1
\frac{4t^2(1+t+t^2)}{(1+t)(1+t^2)}\,dt
=2+\ln2-\frac{\pi}{2}.
$$

Значит:

$$
S=\frac16\left(2+\ln2-\frac{\pi}{2}\right).
$$

Ответ:

$$
S=\frac{2+\ln2-\frac{\pi}{2}}6.
$$

---

## 7. Площадь фигуры в полярных координатах

### 7.1. Полярные координаты

В полярных координатах точка задается парой:

$$
(\rho,\varphi),
$$

где:

- `rho` - расстояние от начала координат;
- `varphi` - угол с положительным направлением оси `Ox`.

Связь с декартовыми координатами:

$$
x=\rho\cos\varphi,
$$

$$
y=\rho\sin\varphi.
$$

### 7.2. Формула площади

Если кривая задана:

$$
\rho=\rho(\varphi),
$$

то площадь сектора между углами `alpha` и `beta`:

$$
S=\frac12\int_\alpha^\beta \rho^2\,d\varphi.
$$

Почему появляется коэффициент `1/2`: площадь сектора круга радиуса `rho` и малого угла `d\varphi` равна примерно:

$$
dS=\frac12\rho^2d\varphi.
$$

### 7.3. Алгоритм решения

1. Определить вид кривой.
2. Найти промежуток изменения угла для одной части фигуры.
3. Записать формулу площади.
4. Если фигура состоит из нескольких одинаковых частей, найти площадь одной части и умножить.
5. Вычислить интеграл.

### 7.4. Пример: `rho=sin 3varphi`

Дано:

$$
\rho=\sin3\varphi.
$$

Это трехлепестковая роза.

Один лепесток получается при:

$$
0\le\varphi\le\frac{\pi}{3}.
$$

Почему: на концах лепестка радиус равен нулю.

При `varphi=0`:

$$
\rho=\sin0=0.
$$

При `varphi=pi/3`:

$$
\rho=\sin\pi=0.
$$

Площадь одного лепестка:

$$
S_1=\frac12\int_0^{\pi/3}\sin^2 3\varphi\,d\varphi.
$$

Делаем замену:

$$
u=3\varphi,\qquad du=3d\varphi,\qquad d\varphi=\frac{du}{3}.
$$

Пределы:

$$
\varphi=0\Rightarrow u=0,
$$

$$
\varphi=\frac{\pi}{3}\Rightarrow u=\pi.
$$

Тогда:

$$
S_1=\frac12\cdot\frac13\int_0^\pi\sin^2u\,du
=\frac16\int_0^\pi\sin^2u\,du.
$$

Используем известный интеграл:

$$
\int_0^\pi\sin^2u\,du=\frac{\pi}{2}.
$$

Получаем:

$$
S_1=\frac16\cdot\frac{\pi}{2}
=\frac{\pi}{12}.
$$

Всего лепестков 3:

$$
S=3S_1=3\cdot\frac{\pi}{12}
=\frac{\pi}{4}.
$$

Ответ:

$$
S=\frac{\pi}{4}.
$$

---

## 8. Приближенное вычисление определенных интегралов

### 8.1. Когда применяются численные методы

Численные методы используются, когда:

1. Первообразная не выражается через элементарные функции.
2. Интеграл слишком сложен для точного вычисления.
3. Функция задана таблицей значений.

Пример:

$$
\int_2^5\frac{dx}{\ln x}.
$$

Функция:

$$
f(x)=\frac1{\ln x}.
$$

У нее нет простой элементарной первообразной, поэтому применяют численные формулы.

### 8.2. Узлы и шаг

Пусть нужно вычислить:

$$
\int_a^bf(x)\,dx.
$$

Делим отрезок `[a,b]` на части.

Узлы:

$$
x_0,x_1,\dots,x_n.
$$

Если промежутков `n`, то узлов `n+1`.

Шаг:

$$
h=\frac{b-a}{n}.
$$

Важно не путать:

- если дано число промежутков `n`, то узлов `n+1`;
- если дано число узлов `m`, то промежутков `m-1`.

### 8.3. Формула прямоугольников

#### Левые прямоугольники

$$
\int_a^bf(x)\,dx\approx h\sum_{k=0}^{n-1}f(x_k).
$$

Смысл: на каждом маленьком отрезке площадь под графиком заменяется площадью прямоугольника, высота которого берется в левой точке.

#### Правые прямоугольники

$$
\int_a^bf(x)\,dx\approx h\sum_{k=1}^{n}f(x_k).
$$

Высота берется в правой точке каждого отрезка.

#### Средние прямоугольники

$$
\int_a^bf(x)\,dx\approx h\sum_{k=0}^{n-1}f\left(\frac{x_k+x_{k+1}}2\right).
$$

Обычно формула средних прямоугольников точнее левых и правых.

### 8.4. Формула трапеций

Формула:

$$
\int_a^bf(x)\,dx\approx
h\left(
\frac{f(x_0)+f(x_n)}2+
\sum_{k=1}^{n-1}f(x_k)
\right).
$$

Смысл: на каждом маленьком отрезке график заменяется прямой, а площадь под прямой является площадью трапеции.

Крайние значения берутся с коэффициентом `1/2`, потому что в сумме площадей трапеций внутренние узлы учитываются дважды, а крайние только один раз.

### 8.5. Формула Симпсона

Формула Симпсона заменяет график на каждом парном наборе промежутков параболой.

Составная формула Симпсона:

$$
\int_a^bf(x)\,dx\approx
\frac{h}{3}
\left(
f(x_0)+f(x_n)
+4\sum_{\substack{k=1\\k\text{ нечетное}}}^{n-1}f(x_k)
+2\sum_{\substack{k=2\\k\text{ четное}}}^{n-2}f(x_k)
\right).
$$

Важно:

```text
число промежутков n должно быть четным.
```

Если число узлов равно 6, то промежутков 5, а 5 - нечетное число. Поэтому стандартную составную формулу Симпсона напрямую применять нельзя.

Если в условии сказано "число узлов 6", корректно применять прямоугольники или трапеции. Для Симпсона нужно либо 7 узлов, либо чтобы было 6 промежутков.

### 8.6. Пример: интеграл от `2` до `5`

Дано:

$$
I=\int_2^5\frac{dx}{\ln x}.
$$

Пусть число узлов равно 6.

Тогда:

$$
x_0=2,\quad x_1=2.6,\quad x_2=3.2,\quad x_3=3.8,\quad x_4=4.4,\quad x_5=5.
$$

Шаг:

$$
h=\frac{5-2}{6-1}=0.6.
$$

Функция:

$$
f(x)=\frac1{\ln x}.
$$

Значения:

$$
f(2)\approx1.4427,
$$

$$
f(2.6)\approx1.0466,
$$

$$
f(3.2)\approx0.8597,
$$

$$
f(3.8)\approx0.7491,
$$

$$
f(4.4)\approx0.6749,
$$

$$
f(5)\approx0.6213.
$$

По формуле трапеций:

$$
I\approx0.6\left(
\frac{1.4427+0.6213}{2}
+1.0466+0.8597+0.7491+0.6749
\right).
$$

Вычисляем:

$$
I\approx2.6174.
$$

Ответ по формуле трапеций:

$$
I\approx2.6174.
$$

Если же в условии имелось в виду 6 промежутков, тогда:

$$
h=\frac{5-2}{6}=0.5.
$$

Тогда можно применить формулу Симпсона, и получится:

$$
I\approx2.5908.
$$

---

## 9. Типичные ошибки

### 9.1. В рядах Тейлора

1. Путают центр разложения.

Если нужно по степеням `x+2`, центр не `2`, а `-2`.

2. Забывают область сходимости.

Для степенного ряда ответ без области сходимости считается неполным.

3. Не проверяют концы интервала.

Если получено условие `|x-a|<R`, то точки `x=a-R` и `x=a+R` нужно проверять отдельно.

4. Неверно подставляют аргумент в стандартный ряд.

Например:

$$
\sin 5x\ne 5\sin x.
$$

Нужно писать:

$$
\sin 5x=\sum_{n=0}^{\infty}(-1)^n\frac{(5x)^{2n+1}}{(2n+1)!}.
$$

### 9.2. В длине дуги

1. Забывают квадрат производной.

Правильно:

$$
\sqrt{1+(y')^2}.
$$

Неправильно:

$$
\sqrt{1+y'}.
$$

2. Для параметрической кривой используют формулу для `y=f(x)`.

Если кривая задана через `t`, нужно использовать:

$$
\sqrt{(x'(t))^2+(y'(t))^2}.
$$

3. Забывают модуль после извлечения корня.

Например:

$$
\sqrt{\sin^2t}=|\sin t|.
$$

### 9.3. В объемах вращения

1. Путают внешний и внутренний радиусы.
2. Забывают квадрат радиуса.
3. Используют не ту ось вращения.
4. Не определяют, какая функция выше.

При вращении вокруг `Ox` радиус - это вертикальное расстояние до оси.

При вращении вокруг `Oy` радиус - это горизонтальное расстояние до оси.

### 9.4. В численном интегрировании

1. Путают число узлов и число промежутков.
2. Применяют формулу Симпсона при нечетном числе промежутков.
3. Неверно считают шаг.
4. Округляют слишком рано.

Лучше все промежуточные вычисления делать с 4-5 знаками после запятой, а ответ округлять в конце.

---

## 10. Краткий алгоритм выбора формулы

Если просят разложить функцию в ряд:

```text
Ищем центр разложения, используем стандартные ряды или формулу Тейлора.
```

Если просят длину дуги:

```text
Смотрим, как задана кривая: y=f(x), параметрически или полярно.
```

Если просят объем вращения:

```text
Определяем ось вращения и радиусы. Чаще всего применяем метод дисков или шайб.
```

Если просят сумму ряда:

```text
Переходим к частичным суммам, раскладываем общий член, ищем телескопическое сокращение или используем известные ряды.
```

Если просят площадь в полярных координатах:

```text
Используем S = 1/2 * integral rho^2 dphi.
```

Если просят приблизительно вычислить интеграл:

```text
Делим отрезок на узлы, считаем шаг и выбираем формулу прямоугольников, трапеций или Симпсона.
```

---

## 11. Минимум, который нужно уверенно сказать на экзамене

### Ряд Тейлора

Ряд Тейлора функции `f` в точке `a` имеет вид:

$$
f(x)=\sum_{n=0}^{\infty}\frac{f^{(n)}(a)}{n!}(x-a)^n.
$$

Если остаточный член стремится к нулю, то ряд сходится к функции.

### Длина дуги

Для параметрической кривой:

$$
L=\int_\alpha^\beta\sqrt{(x'(t))^2+(y'(t))^2}\,dt.
$$

Формула следует из элемента длины:

$$
ds=\sqrt{dx^2+dy^2}.
$$

### Объем вращения

При вращении области между двумя графиками вокруг `Ox`:

$$
V=\pi\int_a^b(R^2-r^2)\,dx.
$$

Здесь `R` - внешний радиус, `r` - внутренний радиус.

### Полярная площадь

$$
S=\frac12\int_\alpha^\beta\rho^2\,d\varphi.
$$

### Численное интегрирование

Формула трапеций:

$$
I\approx h\left(\frac{f(x_0)+f(x_n)}2+\sum_{k=1}^{n-1}f(x_k)\right).
$$

Формула Симпсона:

$$
I\approx\frac h3(f_0+f_n+4f_1+2f_2+4f_3+\dots+2f_{n-2}+4f_{n-1}).
$$

Для Симпсона число промежутков обязательно должно быть четным.
