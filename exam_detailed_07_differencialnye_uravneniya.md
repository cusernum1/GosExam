# ГЭ 7. Дифференциальные уравнения: подробный ответ

Расширенный конспект по матричной экспоненте, методу Лагранжа, задаче Коши и численным методам ОДУ.

---

## 1. Матричная экспонента

Для квадратной матрицы `A` матричная экспонента определяется рядом:

$$
e^{At}=\sum_{k=0}^{\infty}\frac{A^kt^k}{k!}.
$$

Первые члены:

$$
e^{At}=I+At+\frac{A^2t^2}{2!}+\dots
$$

Ряд сходится для любой квадратной матрицы. Это доказывается сравнением с числовым рядом экспоненты через матричную норму.

Свойства:

$$
e^{A0}=I.
$$

$$
\frac{d}{dt}e^{At}=Ae^{At}.
$$

Решение системы:

$$
x'=Ax,\qquad x(0)=x_0
$$

имеет вид:

$$
x(t)=e^{At}x_0.
$$

---

## 2. Метод вариации произвольных постоянных

Для линейного неоднородного уравнения:

$$
y^{(n)}+a_1(x)y^{(n-1)}+\dots+a_n(x)y=f(x)
$$

сначала решают однородное уравнение.

Пусть фундаментальная система:

$$
y_1,\dots,y_n.
$$

Тогда:

$$
y=C_1(x)y_1+\dots+C_n(x)y_n.
$$

Функции `C_i(x)` находят из системы Лагранжа. Идея состоит в том, что постоянные в решении однородного уравнения заменяются функциями.

Метод применим, когда известна фундаментальная система решений однородного уравнения.

---

## 3. Несобственные интегралы

Несобственные интегралы возникают при бесконечном промежутке или особенности функции.

Главный прием - предел.

$$
\int_a^{+\infty}f(x)dx=
\lim_{R\to+\infty}\int_a^R f(x)dx.
$$

Если особенность в конце:

$$
\int_a^b f(x)dx=
\lim_{\varepsilon\to0+}\int_a^{b-\varepsilon}f(x)dx.
$$

Дополнительные приемы:

- замена переменной;
- сравнение;
- интегрирование по частям;
- выделение главной части;
- разбиение интеграла.

---

## 4. Задача Коши и теорема существования

Задача Коши:

$$
y'=f(x,y),\qquad y(x_0)=y_0.
$$

Теорема существования и единственности: если `f` непрерывна и удовлетворяет условию Липшица по `y` в окрестности начальной точки, то существует единственное локальное решение.

Условие Липшица:

$$
|f(x,y_1)-f(x,y_2)|\le L|y_1-y_2|.
$$

Непрерывность обычно обеспечивает существование, а условие Липшица - единственность.

---

## 5. Метод Эйлера

Формула:

$$
y_{k+1}=y_k+h f(x_k,y_k).
$$

Смысл: решение на каждом шаге заменяется касательной.

Локальная погрешность:

```text
O(h^2)
```

Глобальная погрешность:

```text
O(h)
```

Метод прост, но требует малого шага для приемлемой точности.

---

## 6. Модификации Эйлера

Метод средней точки:

$$
y_{k+1}=y_k+h f\left(x_k+\frac{h}{2},y_k+\frac{h}{2}f(x_k,y_k)\right).
$$

Метод Эйлера с пересчетом:

$$
y_{k+1}^{*}=y_k+h f(x_k,y_k).
$$

$$
y_{k+1}=y_k+\frac{h}{2}(f(x_k,y_k)+f(x_{k+1},y_{k+1}^{*})).
$$

Оба метода имеют глобальный порядок `O(h^2)`.

---

## 7. Методы Рунге-Кутты

Общая идея: взять несколько оценок наклона внутри шага и объединить их с весами.

Классический метод четвертого порядка:

$$
k_1=f(x_k,y_k).
$$

$$
k_2=f\left(x_k+\frac{h}{2},y_k+\frac{h}{2}k_1\right).
$$

$$
k_3=f\left(x_k+\frac{h}{2},y_k+\frac{h}{2}k_2\right).
$$

$$
k_4=f(x_k+h,y_k+hk_3).
$$

$$
y_{k+1}=y_k+\frac{h}{6}(k_1+2k_2+2k_3+k_4).
$$

---

## 8. Правило Рунге и автоматический шаг

Если метод порядка `p`, то разность решений с шагом `h` и `h/2` позволяет оценить ошибку:

$$
R=\frac{y_{h/2}-y_h}{2^p-1}.
$$

Автоматический выбор шага:

1. Сделать шаг.
2. Оценить ошибку.
3. Если ошибка велика, уменьшить шаг.
4. Если ошибка мала, увеличить шаг.

Методы Рунге-Кутты-Фелберга и Мерсона используют вложенные формулы разных порядков, чтобы оценивать ошибку без полного пересчета.

---

## 9. Полный устный ответ

Матричная экспонента определяется абсолютно сходящимся рядом и используется для решения линейных систем с постоянной матрицей. Метод вариации произвольных постоянных позволяет решать линейные неоднородные уравнения, если известна фундаментальная система решений однородного уравнения. Задача Коши задает дифференциальное уравнение и начальное условие. Теорема существования и единственности требует непрерывности правой части и условия Липшица. Численные методы решения задачи Коши включают метод Эйлера, его модификации, методы Рунге-Кутты, правило Рунге и автоматический выбор шага.

