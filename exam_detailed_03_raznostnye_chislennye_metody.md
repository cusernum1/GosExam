# ГЭ 3. Разностные численные методы: подробный ответ

Расширенный конспект по численным методам, интерполяции, квадратурам, МНК, ОДУ и УЧП.

---

## 1. Общая идея численных методов

Численные методы нужны, когда точное аналитическое решение невозможно, слишком сложно или не требуется.

Численный метод дает приближенное решение. Поэтому всегда важны:

- погрешность;
- устойчивость;
- сходимость;
- вычислительная сложность;
- влияние округления.

Разностные методы заменяют непрерывные производные конечными разностями на сетке.

---

## 2. Численные методы линейной алгебры

Основная задача:

$$
Ax=b.
$$

Прямые методы:

- метод Гаусса;
- LU-разложение;
- метод Холецкого.

Итерационные методы:

- Якоби;
- Зейдель;
- релаксация;
- сопряженные градиенты.

Прямые методы надежны для умеренных размеров, но могут быть дорогими. Итерационные методы полезны для больших разреженных систем.

Важные понятия:

- невязка `r=b-Ax`;
- обусловленность;
- норма матрицы;
- устойчивость алгоритма.

---

## 3. Интерполяция Лагранжа

Интерполяция строит функцию, точно проходящую через заданные точки.

Многочлен Лагранжа:

$$
L_n(x)=\sum_{k=0}^{n}f(x_k)l_k(x),
$$

где:

$$
l_k(x)=\prod_{j=0,j\ne k}^{n}\frac{x-x_j}{x_k-x_j}.
$$

Свойство:

```text
в узле x_i все базисные многочлены равны 0, кроме l_i, который равен 1.
```

Поэтому `L_n(x_i)=f(x_i)`.

---

## 4. Погрешность интерполяции

Если функция достаточно гладкая, то:

$$
f(x)-L_n(x)=
\frac{f^{(n+1)}(\xi)}{(n+1)!}
\prod_{k=0}^{n}(x-x_k).
$$

Погрешность зависит от:

- гладкости функции;
- расположения узлов;
- степени многочлена;
- расстояния точки от узлов.

Важно: повышение степени многочлена не всегда улучшает результат. На равномерных узлах возможны сильные колебания, известные как эффект Рунге.

---

## 5. Приближение функций и МНК

Интерполяция требует точного прохождения через точки. Аппроксимация допускает отклонение и выбирает функцию, которая в целом лучше описывает данные.

Метод наименьших квадратов минимизирует сумму квадратов ошибок:

$$
S=\sum_{i=1}^{n}(y_i-P(x_i))^2.
$$

Для многочлена:

$$
P_m(x)=a_0+a_1x+\dots+a_mx^m.
$$

Необходимые условия минимума:

$$
\frac{\partial S}{\partial a_j}=0.
$$

Они дают нормальную систему.

### Линеаризация

Некоторые нелинейные модели можно привести к линейным.

Пример:

```text
y = ae^{bx}
ln y = ln a + bx
```

После логарифмирования можно применить линейный МНК.

---

## 6. Численное интегрирование

Квадратурная формула:

$$
\int_a^b f(x)dx\approx \sum A_k f(x_k).
$$

Формулы прямоугольников:

- левые;
- правые;
- средние.

Средние прямоугольники обычно точнее левых и правых, потому что имеют порядок `O(h^2)`.

Формула Симпсона:

$$
\int_a^b f(x)dx\approx
\frac{b-a}{6}\left(f(a)+4f\left(\frac{a+b}{2}\right)+f(b)\right).
$$

Составная формула Симпсона требует четного числа промежутков и имеет порядок `O(h^4)`.

Правило Рунге:

$$
R\approx \frac{I_{h/2}-I_h}{2^p-1}.
$$

---

## 7. Формулы Гаусса и многочлены Лежандра

Алгебраическая степень точности - максимальная степень многочлена, для которого квадратурная формула точна.

Формулы Гаусса имеют наивысшую степень точности при заданном числе узлов.

Для `n` узлов степень точности:

$$
2n-1.
$$

На отрезке `[-1,1]` узлы формулы Гаусса являются корнями многочлена Лежандра.

Многочлены Лежандра ортогональны:

$$
\int_{-1}^{1}P_m(x)P_n(x)dx=0
$$

при разных `m` и `n`.

---

## 8. Несобственные интегралы

Несобственный интеграл требует предельного перехода.

Для бесконечного промежутка:

$$
\int_a^{+\infty}f(x)dx=
\lim_{R\to+\infty}\int_a^R f(x)dx.
$$

Для особенности в конце:

$$
\int_a^b f(x)dx=
\lim_{\varepsilon\to0+}\int_a^{b-\varepsilon}f(x)dx.
$$

Приемы:

- замена переменной;
- выделение особенности;
- разбиение области;
- сравнение;
- численное интегрирование после преобразования.

---

## 9. Численное решение ОДУ

Задача Коши:

$$
y'=f(x,y),\qquad y(x_0)=y_0.
$$

Метод Эйлера:

$$
y_{k+1}=y_k+h f(x_k,y_k).
$$

Локальная погрешность `O(h^2)`, глобальная `O(h)`.

Методы Рунге-Кутты используют несколько промежуточных наклонов. Классический метод четвертого порядка имеет глобальную погрешность `O(h^4)`.

---

## 10. Численное решение УЧП

Для УЧП строят сетку и заменяют производные разностями.

Пример для теплопроводности:

$$
u_t=a^2u_{xx}.
$$

Явная схема:

$$
\frac{u_i^{j+1}-u_i^j}{\tau}
=a^2\frac{u_{i+1}^j-2u_i^j+u_{i-1}^j}{h^2}.
$$

Основные свойства схем:

- аппроксимация;
- устойчивость;
- сходимость.

Без устойчивости маленькие ошибки могут быстро расти.

---

## 11. Полный устный ответ

Разностные численные методы заменяют непрерывную задачу конечной дискретной задачей. В линейной алгебре применяются прямые и итерационные методы решения систем. Интерполяция Лагранжа строит многочлен, проходящий через заданные точки, а погрешность зависит от производной высокого порядка и расположения узлов. Метод наименьших квадратов строит приближение, минимизируя сумму квадратов отклонений. Численное интегрирование использует квадратурные формулы: прямоугольников, Симпсона, Гаусса. Для ОДУ применяются методы Эйлера и Рунге-Кутты, для УЧП - разностные схемы, которые оцениваются по аппроксимации, устойчивости и сходимости.

