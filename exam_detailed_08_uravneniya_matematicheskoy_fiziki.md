# ГЭ 8. Уравнения математической физики: подробный ответ

Расширенный конспект по волновому уравнению, теплопроводности и гармоническим функциям.

---

## 1. Задача Коши для колебания струны

Уравнение:

$$
u_{tt}=a^2u_{xx}.
$$

Оно описывает поперечные колебания бесконечной струны.

Задача Коши:

$$
u(x,0)=\varphi(x).
$$

$$
u_t(x,0)=\psi(x).
$$

Первая функция задает начальное положение струны, вторая - начальную скорость.

---

## 2. Метод бегущих волн

Общее решение:

$$
u(x,t)=F(x-at)+G(x+at).
$$

`F(x-at)` - волна вправо. `G(x+at)` - волна влево.

Формула Даламбера:

$$
u(x,t)=\frac{\varphi(x-at)+\varphi(x+at)}{2}
+\frac{1}{2a}\int_{x-at}^{x+at}\psi(s)ds.
$$

Смысл: точка струны в момент `t` зависит только от начальных данных на конечном отрезке.

---

## 3. Смешанная задача для струны

На отрезке:

$$
0<x<l.
$$

Уравнение:

$$
u_{tt}=a^2u_{xx}.
$$

Закрепленные концы:

$$
u(0,t)=u(l,t)=0.
$$

Начальные условия:

$$
u(x,0)=\varphi(x),\qquad u_t(x,0)=\psi(x).
$$

Метод разделения переменных ищет:

$$
u=X(x)T(t).
$$

Получается задача Штурма-Лиувилля:

$$
X''+\lambda X=0,\qquad X(0)=X(l)=0.
$$

Собственные функции:

$$
\sin\frac{n\pi x}{l}.
$$

Решение:

$$
u(x,t)=\sum_{n=1}^{\infty}
\left(A_n\cos\frac{an\pi t}{l}+B_n\sin\frac{an\pi t}{l}\right)
\sin\frac{n\pi x}{l}.
$$

---

## 4. Теорема максимума для теплопроводности

Уравнение:

$$
u_t=a^2u_{xx}.
$$

Теорема: максимум и минимум решения в цилиндрической области достигаются на параболической границе, если решение не является постоянным.

Для отрезка параболическая граница:

- начальный момент `t=0`;
- боковые границы `x=0` и `x=l`.

Следствия:

- единственность решения;
- устойчивость по данным;
- сохранение знака решения.

Почему это важно: если две функции решают одну задачу, их разность решает задачу с нулевыми данными. По теореме максимума эта разность равна нулю.

---

## 5. Задача Коши для теплопроводности

Уравнение:

$$
u_t=a^2u_{xx}.
$$

Начальное условие:

$$
u(x,0)=\varphi(x).
$$

Интеграл Пуассона:

$$
u(x,t)=\frac{1}{2a\sqrt{\pi t}}
\int_{-\infty}^{+\infty}
e^{-(x-\xi)^2/(4a^2t)}\varphi(\xi)d\xi.
$$

Это свертка начальной функции с тепловым ядром.

Смысл: тепло распространяется мгновенно в математической модели, но вклад дальних точек быстро убывает из-за экспоненты.

---

## 6. Гармонические функции

Гармоническая функция удовлетворяет:

$$
\Delta u=0.
$$

Гармонические функции возникают в стационарных задачах теплопроводности, электростатике, гидродинамике.

Основные свойства:

- бесконечная гладкость внутри области;
- принцип максимума;
- значение внутри области определяется граничными значениями при корректной постановке задачи Дирихле.

---

## 7. Интегральная формула Пуассона для круга

Если функция гармонична в круге радиуса `R`, и:

$$
u(R,\theta)=f(\theta),
$$

то:

$$
u(r,\varphi)=
\frac{1}{2\pi}\int_0^{2\pi}
\frac{R^2-r^2}{R^2-2Rr\cos(\varphi-\theta)+r^2}
f(\theta)d\theta.
$$

Ядро Пуассона играет роль веса, с которым усредляются граничные значения.

---

## 8. Полный устный ответ

Уравнения математической физики описывают типовые физические процессы. Волновое уравнение описывает колебания струны; для задачи Коши используется метод бегущих волн и формула Даламбера. Для конечной струны применяется разделение переменных и разложение по синусам. Уравнение теплопроводности описывает диффузионный процесс; для него справедлива теорема максимума и минимума, обеспечивающая единственность и устойчивость. Задача Коши для теплопроводности решается интегралом Пуассона. Гармонические функции удовлетворяют уравнению Лапласа, а их значения внутри круга выражаются через граничные значения интегральной формулой Пуассона.

