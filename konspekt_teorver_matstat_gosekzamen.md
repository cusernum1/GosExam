# Конспект по теории вероятностей и математической статистике для госэкзамена

Этот файл сделан как учебный конспект для подготовки к смешанному госэкзамену. Материал изложен подробно: сначала смысл понятий, затем формулы, условия применимости, алгоритмы решения и разобранные примеры.

Конспект ориентирован на учебную литературу, указанную в программе:

Основная электронная учебно-методическая документация:

1. Емельянов Г.В., Скитович В.П. Задачник по теории вероятностей и математической статистике. СПб.: Лань, 2007. 336 с.

Дополнительная электронная учебно-методическая документация:

2. Хрущева И.В. Теория вероятностей. СПб.: Лань, 2009. 304 с.

Основная печатная учебно-методическая документация:

3. Гнеденко Б.В. Курс теории вероятностей: учебник для математических специальностей университетов. 10-е изд., доп. М.: URSS; ЛИБРОКОМ, 2011. 485 с.

Дополнительная печатная учебно-методическая документация:

4. Свешников А.А. Прикладные методы теории случайных функций. 2-е изд., перераб. и доп. М.: Наука, 1968. 463 с.
5. Гмурман В.Е. Руководство к решению задач по теории вероятностей и математической статистике. 5-е изд., стер. М.: Высшая школа, 2001. 400 с.

Материал ниже написан своими словами в учебном формате подготовки к госэкзамену.

---

## 1. Случайные события и вероятность

### 1.1. Случайный эксперимент

Случайным экспериментом называется опыт, результат которого заранее точно предсказать нельзя, но множество возможных исходов известно или может быть описано.

Примеры:

- бросание монеты;
- бросание игральной кости;
- выбор детали из партии;
- измерение времени работы прибора;
- наблюдение случайной величины в статистической выборке.

Множество всех элементарных исходов называют пространством элементарных исходов и обозначают:

```math
\Omega.
```

Элементарный исход обычно обозначают:

```math
\omega.
```

Событие - это подмножество пространства элементарных исходов:

```math
A\subseteq\Omega.
```

Если в результате опыта произошел исход, принадлежащий множеству `A`, говорят, что событие `A` произошло.

### 1.2. Операции над событиями

Пусть `A` и `B` - события.

Сумма событий:

```math
A\cup B.
```

Это событие означает: произошло `A` или произошло `B`, включая случай, когда произошли оба.

Произведение событий:

```math
A\cap B.
```

Это событие означает: произошли одновременно `A` и `B`.

Противоположное событие:

```math
\overline{A}.
```

Это событие означает: событие `A` не произошло.

Разность событий:

```math
A\setminus B.
```

Это означает: произошло `A`, но не произошло `B`.

Несовместные события:

```math
A\cap B=\varnothing.
```

То есть они не могут произойти одновременно.

### 1.3. Классическое определение вероятности

Если пространство элементарных исходов конечно и все исходы равновозможны, то вероятность события `A` определяется формулой:

```math
P(A)=\frac{m}{n},
```

где:

- `n` - число всех равновозможных исходов;
- `m` - число исходов, благоприятствующих событию `A`.

Важно: классическое определение применимо только при равновозможных исходах.

### 1.4. Геометрическое определение вероятности

Если исход случайно выбирается из некоторой области `G`, а событию `A` соответствует подобласть `g`, то:

```math
P(A)=\frac{\operatorname{mes} g}{\operatorname{mes} G}.
```

Здесь `mes` означает меру: длину, площадь или объем.

Например, если точка случайно выбирается на отрезке, то вероятность попадания в часть отрезка равна отношению длин.

### 1.5. Аксиомы вероятности

Вероятность - это функция `P`, которая каждому событию ставит в соответствие число и удовлетворяет аксиомам Колмогорова.

1. Неотрицательность:

```math
P(A)\ge 0.
```

2. Нормировка:

```math
P(\Omega)=1.
```

3. Счетная аддитивность: если события `A_1,A_2,...` попарно несовместны, то:

```math
P\left(\bigcup_{k=1}^{\infty}A_k\right)
=\sum_{k=1}^{\infty}P(A_k).
```

Из этих аксиом следуют основные свойства.

### 1.6. Основные свойства вероятности

Вероятность невозможного события:

```math
P(\varnothing)=0.
```

Вероятность противоположного события:

```math
P(\overline A)=1-P(A).
```

Если `A\subseteq B`, то:

```math
P(A)\le P(B).
```

Формула сложения вероятностей:

```math
P(A\cup B)=P(A)+P(B)-P(A\cap B).
```

Если события несовместны, то `P(A\cap B)=0`, и формула принимает вид:

```math
P(A\cup B)=P(A)+P(B).
```

---

## 2. Условная вероятность, независимость и формула Байеса

### 2.1. Условная вероятность

Условная вероятность события `A` при условии, что событие `B` произошло, определяется так:

```math
P(A|B)=\frac{P(A\cap B)}{P(B)},
\qquad P(B)>0.
```

Смысл: мы ограничиваем пространство исходов только теми исходами, где произошло `B`, и смотрим, какая часть из них соответствует событию `A`.

Из определения следует формула умножения:

```math
P(A\cap B)=P(B)P(A|B).
```

Также:

```math
P(A\cap B)=P(A)P(B|A).
```

### 2.2. Независимые события

События `A` и `B` называются независимыми, если наступление одного не изменяет вероятность другого:

```math
P(A|B)=P(A).
```

Эквивалентная формула:

```math
P(A\cap B)=P(A)P(B).
```

Важно: независимость и несовместность - разные понятия.

Если два события несовместны, то они не могут произойти вместе. Если они независимы, то появление одного не влияет на вероятность другого.

### 2.3. Формула полной вероятности

Пусть события `H_1,H_2,\dots,H_n` образуют полную группу событий:

```math
H_i\cap H_j=\varnothing,\quad i\ne j,
```

```math
H_1\cup H_2\cup\dots\cup H_n=\Omega,
```

и:

```math
P(H_i)>0.
```

Тогда для любого события `A`:

```math
P(A)=\sum_{i=1}^nP(H_i)P(A|H_i).
```

Смысл: событие `A` может произойти через разные гипотезы `H_i`; складываем вероятности всех таких вариантов.

### 2.4. Формула Байеса

Если событие `A` произошло, то вероятность гипотезы `H_k` после наступления `A` равна:

```math
P(H_k|A)=
\frac{P(H_k)P(A|H_k)}
{\sum_{i=1}^nP(H_i)P(A|H_i)}.
```

Формула Байеса позволяет пересчитать вероятности гипотез после получения новой информации.

---

## 3. Случайные величины

### 3.1. Определение случайной величины

Случайная величина - это числовая функция, заданная на пространстве элементарных исходов:

```math
X=X(\omega).
```

То есть каждому исходу эксперимента ставится в соответствие число.

Примеры:

- число выпавших очков на кости;
- число отказавших приборов;
- время ожидания;
- рост случайно выбранного человека;
- ошибка измерения.

### 3.2. Дискретная случайная величина

Случайная величина называется дискретной, если она принимает конечное или счетное множество значений:

```math
x_1,x_2,\dots
```

Закон распределения дискретной случайной величины задается вероятностями:

```math
P(X=x_i)=p_i.
```

Обязательно:

```math
p_i\ge 0,
```

```math
\sum_i p_i=1.
```

Таблица распределения имеет вид:

```math
\begin{array}{c|cccc}
X & x_1 & x_2 & \dots & x_n\\
\hline
P & p_1 & p_2 & \dots & p_n
\end{array}
```

### 3.3. Непрерывная случайная величина

Случайная величина называется непрерывной, если ее распределение задается плотностью `f(x)`.

Плотность должна удовлетворять условиям:

```math
f(x)\ge 0
```

для всех `x`, и:

```math
\int_{-\infty}^{+\infty}f(x)\,dx=1.
```

Вероятность попадания в интервал:

```math
P(a<X<b)=\int_a^b f(x)\,dx.
```

Для непрерывной случайной величины:

```math
P(X=a)=0.
```

Это важное отличие от дискретного случая.

---

## 4. Функция распределения

### 4.1. Определение

Функцией распределения случайной величины `X` называется функция:

```math
F(x)=P(X<x)
```

или в некоторых учебниках:

```math
F(x)=P(X\le x).
```

В большинстве задач разница важна только в точках скачка. Для непрерывных распределений разницы нет.

### 4.2. Свойства функции распределения

1. Функция распределения неубывает.

Если `x_1<x_2`, то:

```math
F(x_1)\le F(x_2).
```

2. Значения функции распределения находятся от `0` до `1`:

```math
0\le F(x)\le 1.
```

3. На минус бесконечности:

```math
\lim_{x\to-\infty}F(x)=0.
```

4. На плюс бесконечности:

```math
\lim_{x\to+\infty}F(x)=1.
```

5. Для непрерывной случайной величины с плотностью:

```math
F(x)=\int_{-\infty}^{x}f(t)\,dt.
```

6. Если `F` дифференцируема, то:

```math
f(x)=F'(x).
```

### 4.3. Как находить функцию распределения по плотности

Алгоритм:

1. Проверить, что `f(x)` действительно является плотностью.
2. Найти область, где плотность не равна нулю.
3. Для `x` левее этой области написать `F(x)=0`.
4. Для `x` внутри области вычислить интеграл:

```math
F(x)=\int_{a}^{x}f(t)\,dt.
```

5. Для `x` правее области написать `F(x)=1`.

---

## 5. Числовые характеристики случайных величин

### 5.1. Математическое ожидание

Математическое ожидание - это среднее значение случайной величины в вероятностном смысле.

Для дискретной случайной величины:

```math
M(X)=E(X)=\sum_i x_i p_i.
```

Здесь каждое значение `x_i` умножается на вероятность его появления.

Для непрерывной случайной величины:

```math
M(X)=E(X)=\int_{-\infty}^{+\infty}x f(x)\,dx.
```

### 5.2. Свойства математического ожидания

```math
E(C)=C.
```

```math
E(CX)=C E(X).
```

```math
E(X+Y)=E(X)+E(Y).
```

Если `X` и `Y` независимы, то:

```math
E(XY)=E(X)E(Y).
```

Последнее свойство в общем случае без независимости неверно.

### 5.3. Дисперсия

Дисперсия характеризует разброс значений случайной величины около математического ожидания.

Определение:

```math
D(X)=E\left[(X-E(X))^2\right].
```

На практике чаще используют формулу:

```math
D(X)=E(X^2)-[E(X)]^2.
```

Для дискретной случайной величины:

```math
E(X^2)=\sum_i x_i^2 p_i.
```

Для непрерывной:

```math
E(X^2)=\int_{-\infty}^{+\infty}x^2f(x)\,dx.
```

### 5.4. Среднее квадратическое отклонение

Среднее квадратическое отклонение:

```math
\sigma(X)=\sqrt{D(X)}.
```

Оно измеряется в тех же единицах, что и сама случайная величина.

### 5.5. Ковариация

Ковариация двух случайных величин показывает направление совместного изменения.

Определение:

```math
\operatorname{cov}(X,Y)=E[(X-E(X))(Y-E(Y))].
```

Удобная формула:

```math
\operatorname{cov}(X,Y)=E(XY)-E(X)E(Y).
```

Если ковариация положительна, то величины в среднем изменяются в одном направлении.

Если ковариация отрицательна, то при увеличении одной другая обычно имеет тенденцию уменьшаться.

Если ковариация равна нулю, линейной зависимости может не быть, но это не всегда означает независимость.

### 5.6. Коэффициент корреляции

Коэффициент корреляции:

```math
r_{XY}=\frac{\operatorname{cov}(X,Y)}{\sigma_X\sigma_Y}.
```

Так как:

```math
\sigma_X=\sqrt{D(X)},\qquad \sigma_Y=\sqrt{D(Y)},
```

то:

```math
r_{XY}=\frac{E(XY)-E(X)E(Y)}{\sqrt{D(X)}\sqrt{D(Y)}}.
```

Свойство:

```math
-1\le r_{XY}\le 1.
```

Если `r` близок к `1`, связь положительная и сильная.

Если `r` близок к `-1`, связь отрицательная и сильная.

Если `r` близок к `0`, линейная связь слабая или отсутствует.

---

## 6. Система двух случайных величин

### 6.1. Совместное распределение

Если рассматриваются две случайные величины `X` и `Y`, то их совместное распределение задает вероятности одновременного принятия значений:

```math
p_{ij}=P(X=x_i,\ Y=y_j).
```

Для дискретной системы распределение обычно задается таблицей.

Все вероятности должны удовлетворять условиям:

```math
p_{ij}\ge 0,
```

```math
\sum_i\sum_j p_{ij}=1.
```

### 6.2. Маргинальные распределения

Из совместного распределения можно получить отдельные распределения `X` и `Y`.

Для `X`:

```math
P(X=x_i)=\sum_j p_{ij}.
```

То есть нужно сложить вероятности по всем значениям `Y` при фиксированном `x_i`.

Для `Y`:

```math
P(Y=y_j)=\sum_i p_{ij}.
```

То есть нужно сложить вероятности по всем значениям `X` при фиксированном `y_j`.

### 6.3. Независимость случайных величин

Случайные величины `X` и `Y` независимы, если для всех `i,j`:

```math
P(X=x_i,\ Y=y_j)=P(X=x_i)P(Y=y_j).
```

Если хотя бы для одной пары равенство нарушается, величины зависимы.

### 6.4. Алгоритм решения задач по таблице совместного распределения

1. Проверить, что сумма всех вероятностей равна `1`.
2. Найти распределение `X`: сложить вероятности по столбцам.
3. Найти распределение `Y`: сложить вероятности по строкам.
4. Найти `E(X)` и `E(Y)`.
5. Найти `E(X^2)` и `E(Y^2)`.
6. Найти дисперсии:

```math
D(X)=E(X^2)-[E(X)]^2,
```

```math
D(Y)=E(Y^2)-[E(Y)]^2.
```

7. Найти `E(XY)` по совместной таблице:

```math
E(XY)=\sum_i\sum_j x_i y_j p_{ij}.
```

8. Найти ковариацию:

```math
\operatorname{cov}(X,Y)=E(XY)-E(X)E(Y).
```

9. Найти коэффициент корреляции:

```math
r_{XY}=
\frac{\operatorname{cov}(X,Y)}{\sqrt{D(X)}\sqrt{D(Y)}}.
```

---

## 7. Подробный разбор задачи 1 с изображения

### 7.1. Условие

Закон распределения системы двух случайных величин `(X,Y)` задан таблицей.

Значения `X`:

```math
1,\ 3,\ 5,\ 7.
```

Значения `Y`:

```math
-2,\ 0,\ 2.
```

Таблица вероятностей:

```math
\begin{array}{c|cccc}
Y\backslash X & 1 & 3 & 5 & 7\\
\hline
-2 & 0.04 & 0.06 & 0.10 & 0.08\\
0  & 0.06 & 0.07 & 0.15 & 0.16\\
2  & 0.05 & 0.10 & 0.12 & 0.01
\end{array}
```

Найти:

```math
E(X),\ E(Y),\ D(X),\ D(Y),\ r_{XY}.
```

### 7.2. Проверка таблицы распределения

Сначала нужно проверить, что сумма всех вероятностей равна единице.

Суммы по строкам:

```math
0.04+0.06+0.10+0.08=0.28.
```

```math
0.06+0.07+0.15+0.16=0.44.
```

```math
0.05+0.10+0.12+0.01=0.28.
```

Сумма всех вероятностей:

```math
0.28+0.44+0.28=1.
```

Значит, таблица действительно задает закон распределения.

### 7.3. Распределение случайной величины `X`

Чтобы найти вероятность `P(X=x_i)`, нужно сложить элементы соответствующего столбца.

Для `X=1`:

```math
P(X=1)=0.04+0.06+0.05=0.15.
```

Для `X=3`:

```math
P(X=3)=0.06+0.07+0.10=0.23.
```

Для `X=5`:

```math
P(X=5)=0.10+0.15+0.12=0.37.
```

Для `X=7`:

```math
P(X=7)=0.08+0.16+0.01=0.25.
```

Получаем распределение:

```math
\begin{array}{c|cccc}
X & 1 & 3 & 5 & 7\\
\hline
P & 0.15 & 0.23 & 0.37 & 0.25
\end{array}
```

Проверка:

```math
0.15+0.23+0.37+0.25=1.
```

### 7.4. Распределение случайной величины `Y`

Чтобы найти вероятность `P(Y=y_j)`, нужно сложить элементы соответствующей строки.

Для `Y=-2`:

```math
P(Y=-2)=0.04+0.06+0.10+0.08=0.28.
```

Для `Y=0`:

```math
P(Y=0)=0.06+0.07+0.15+0.16=0.44.
```

Для `Y=2`:

```math
P(Y=2)=0.05+0.10+0.12+0.01=0.28.
```

Получаем:

```math
\begin{array}{c|ccc}
Y & -2 & 0 & 2\\
\hline
P & 0.28 & 0.44 & 0.28
\end{array}
```

### 7.5. Математическое ожидание `X`

Формула:

```math
E(X)=\sum_i x_iP(X=x_i).
```

Подставляем:

```math
E(X)=1\cdot0.15+3\cdot0.23+5\cdot0.37+7\cdot0.25.
```

Считаем:

```math
1\cdot0.15=0.15,
```

```math
3\cdot0.23=0.69,
```

```math
5\cdot0.37=1.85,
```

```math
7\cdot0.25=1.75.
```

Тогда:

```math
E(X)=0.15+0.69+1.85+1.75=4.44.
```

### 7.6. Математическое ожидание `Y`

```math
E(Y)=(-2)\cdot0.28+0\cdot0.44+2\cdot0.28.
```

```math
E(Y)=-0.56+0+0.56=0.
```

### 7.7. Дисперсия `X`

Сначала находим:

```math
E(X^2)=\sum_i x_i^2P(X=x_i).
```

Подставляем:

```math
E(X^2)=1^2\cdot0.15+3^2\cdot0.23+5^2\cdot0.37+7^2\cdot0.25.
```

```math
E(X^2)=0.15+9\cdot0.23+25\cdot0.37+49\cdot0.25.
```

```math
E(X^2)=0.15+2.07+9.25+12.25=23.72.
```

Формула дисперсии:

```math
D(X)=E(X^2)-[E(X)]^2.
```

```math
D(X)=23.72-(4.44)^2.
```

```math
(4.44)^2=19.7136.
```

```math
D(X)=23.72-19.7136=4.0064.
```

### 7.8. Дисперсия `Y`

```math
E(Y^2)=(-2)^2\cdot0.28+0^2\cdot0.44+2^2\cdot0.28.
```

```math
E(Y^2)=4\cdot0.28+0+4\cdot0.28=1.12+1.12=2.24.
```

Так как:

```math
E(Y)=0,
```

то:

```math
D(Y)=E(Y^2)-[E(Y)]^2=2.24-0^2=2.24.
```

### 7.9. Математическое ожидание произведения `XY`

Формула:

```math
E(XY)=\sum_i\sum_j x_i y_j p_{ij}.
```

То есть каждый элемент таблицы умножается на соответствующие значения `x_i` и `y_j`.

Считаем по строкам.

Для `Y=-2`:

```math
(-2)(1\cdot0.04+3\cdot0.06+5\cdot0.10+7\cdot0.08).
```

Внутри скобок:

```math
1\cdot0.04+3\cdot0.06+5\cdot0.10+7\cdot0.08
=0.04+0.18+0.50+0.56=1.28.
```

Вклад строки:

```math
-2\cdot1.28=-2.56.
```

Для `Y=0` вклад равен нулю, потому что все произведения содержат множитель `0`.

Для `Y=2`:

```math
2(1\cdot0.05+3\cdot0.10+5\cdot0.12+7\cdot0.01).
```

Внутри скобок:

```math
0.05+0.30+0.60+0.07=1.02.
```

Вклад строки:

```math
2\cdot1.02=2.04.
```

Итого:

```math
E(XY)=-2.56+0+2.04=-0.52.
```

### 7.10. Ковариация

Формула:

```math
\operatorname{cov}(X,Y)=E(XY)-E(X)E(Y).
```

Подставляем:

```math
\operatorname{cov}(X,Y)=-0.52-4.44\cdot0=-0.52.
```

### 7.11. Коэффициент корреляции

Формула:

```math
r_{XY}=
\frac{\operatorname{cov}(X,Y)}{\sqrt{D(X)}\sqrt{D(Y)}}.
```

Подставляем:

```math
r_{XY}=
\frac{-0.52}{\sqrt{4.0064}\sqrt{2.24}}.
```

```math
\sqrt{4.0064}\approx2.0016,
```

```math
\sqrt{2.24}\approx1.4967.
```

```math
r_{XY}\approx
\frac{-0.52}{2.0016\cdot1.4967}
\approx -0.1736.
```

Ответ:

```math
E(X)=4.44,
```

```math
E(Y)=0,
```

```math
D(X)=4.0064,
```

```math
D(Y)=2.24,
```

```math
r_{XY}\approx -0.1736.
```

Смысл ответа: связь между `X` и `Y` слабая отрицательная, потому что коэффициент корреляции близок к нулю и имеет отрицательный знак.

---

## 8. Подробный разбор задачи 2 с изображения

### 8.1. Условие

Плотность распределения случайной величины задана функцией:

```math
f(x)=
\begin{cases}
\cos 3x, & x\in\left[0,\frac{\pi}{3}\right],\\
0, & x\notin\left[0,\frac{\pi}{3}\right].
\end{cases}
```

Найти функцию распределения случайной величины.

### 8.2. Важная проверка: является ли это плотностью

Прежде чем искать функцию распределения, нужно проверить два условия плотности:

1. `f(x)>=0`.
2. Полный интеграл равен `1`:

```math
\int_{-\infty}^{+\infty}f(x)\,dx=1.
```

Проверим первое условие.

На отрезке:

```math
0\le x\le\frac{\pi}{3}
```

аргумент `3x` изменяется от `0` до `pi`.

Функция `cos 3x` положительна на:

```math
0\le x<\frac{\pi}{6},
```

равна нулю при:

```math
x=\frac{\pi}{6},
```

и отрицательна на:

```math
\frac{\pi}{6}<x\le\frac{\pi}{3}.
```

Значит, условие неотрицательности нарушено.

Проверим интеграл:

```math
\int_0^{\pi/3}\cos3x\,dx
=\left[\frac{\sin3x}{3}\right]_0^{\pi/3}.
```

Подставляем пределы:

```math
\frac{\sin\pi}{3}-\frac{\sin0}{3}=0-0=0.
```

Но для плотности должно быть:

```math
\int_{-\infty}^{+\infty}f(x)\,dx=1.
```

Следовательно, функция, записанная в условии, не является плотностью распределения.

### 8.3. Корректный вывод по строгому решению

Так как `cos 3x` на отрезке `[0, pi/3]` принимает отрицательные значения и интеграл по всей области равен `0`, такая функция не может быть плотностью распределения случайной величины.

Поэтому строгий ответ:

```text
Функция распределения для данной "плотности" не существует, так как заданная функция не удовлетворяет условиям плотности.
```

### 8.4. Что делать, если на экзамене в условии опечатка

Иногда в подобных задачах встречается опечатка. Нужно спокойно сказать:

```text
Сначала проверим, является ли функция плотностью.
```

После проверки видно, что условие некорректно.

Если преподаватель разрешит считать формально, то можно записать формальную первообразную:

```math
F(x)=\int_{-\infty}^{x}f(t)\,dt.
```

Тогда:

```math
F(x)=0,\qquad x<0.
```

Для:

```math
0\le x\le\frac{\pi}{3}
```

формально:

```math
F(x)=\int_0^x\cos3t\,dt
=\left[\frac{\sin3t}{3}\right]_0^x
=\frac{\sin3x}{3}.
```

Для:

```math
x>\frac{\pi}{3}
```

формально:

```math
F(x)=\int_0^{\pi/3}\cos3t\,dt=0.
```

Но такая функция не является функцией распределения, потому что она не является неубывающей и не стремится к `1` при `x\to+\infty`.

### 8.5. Пример исправленного варианта

Если бы плотность была задана, например, так:

```math
f(x)=
\begin{cases}
3\cos3x, & 0\le x\le\frac{\pi}{6},\\
0, & x\notin\left[0,\frac{\pi}{6}\right],
\end{cases}
```

то это была бы корректная плотность, потому что:

```math
\int_0^{\pi/6}3\cos3x\,dx
=\left[\sin3x\right]_0^{\pi/6}
=\sin\frac{\pi}{2}-\sin0
=1.
```

Тогда функция распределения была бы:

```math
F(x)=
\begin{cases}
0, & x<0,\\
\sin3x, & 0\le x\le\frac{\pi}{6},\\
1, & x>\frac{\pi}{6}.
\end{cases}
```

---

## 9. Основные дискретные распределения

### 9.1. Бернуллиевское распределение

Случайная величина `X` имеет распределение Бернулли, если она принимает два значения:

```math
X=
\begin{cases}
1, & \text{успех},\\
0, & \text{неудача}.
\end{cases}
```

Вероятности:

```math
P(X=1)=p,
```

```math
P(X=0)=q=1-p.
```

Математическое ожидание:

```math
E(X)=p.
```

Дисперсия:

```math
D(X)=pq.
```

### 9.2. Биномиальное распределение

Если проводится `n` независимых испытаний Бернулли, вероятность успеха в каждом испытании равна `p`, то число успехов `X` имеет биномиальное распределение:

```math
P(X=k)=C_n^k p^k q^{n-k},
\qquad k=0,1,\dots,n.
```

Здесь:

```math
q=1-p.
```

Математическое ожидание:

```math
E(X)=np.
```

Дисперсия:

```math
D(X)=npq.
```

### 9.3. Геометрическое распределение

Если `X` - номер первого успешного испытания, то:

```math
P(X=k)=q^{k-1}p,
\qquad k=1,2,\dots
```

Математическое ожидание:

```math
E(X)=\frac1p.
```

Дисперсия:

```math
D(X)=\frac{q}{p^2}.
```

### 9.4. Распределение Пуассона

Случайная величина `X` имеет распределение Пуассона с параметром `lambda`, если:

```math
P(X=k)=\frac{\lambda^k}{k!}e^{-\lambda},
\qquad k=0,1,2,\dots
```

Математическое ожидание:

```math
E(X)=\lambda.
```

Дисперсия:

```math
D(X)=\lambda.
```

Распределение Пуассона часто используется для числа редких событий за фиксированный промежуток времени или на фиксированной области.

---

## 10. Основные непрерывные распределения

### 10.1. Равномерное распределение

Случайная величина `X` равномерно распределена на `[a,b]`, если:

```math
f(x)=
\begin{cases}
\frac{1}{b-a}, & a\le x\le b,\\
0, & x\notin[a,b].
\end{cases}
```

Математическое ожидание:

```math
E(X)=\frac{a+b}{2}.
```

Дисперсия:

```math
D(X)=\frac{(b-a)^2}{12}.
```

### 10.2. Показательное распределение

Случайная величина имеет показательное распределение с параметром `lambda>0`, если:

```math
f(x)=
\begin{cases}
\lambda e^{-\lambda x}, & x\ge0,\\
0, & x<0.
\end{cases}
```

Функция распределения:

```math
F(x)=
\begin{cases}
0, & x<0,\\
1-e^{-\lambda x}, & x\ge0.
\end{cases}
```

Математическое ожидание:

```math
E(X)=\frac1\lambda.
```

Дисперсия:

```math
D(X)=\frac1{\lambda^2}.
```

### 10.3. Нормальное распределение

Случайная величина `X` имеет нормальное распределение с параметрами `a` и `sigma`, если плотность:

```math
f(x)=\frac{1}{\sigma\sqrt{2\pi}}
e^{-\frac{(x-a)^2}{2\sigma^2}},
\qquad \sigma>0.
```

Обозначение:

```math
X\sim N(a,\sigma^2).
```

Математическое ожидание:

```math
E(X)=a.
```

Дисперсия:

```math
D(X)=\sigma^2.
```

Стандартное нормальное распределение:

```math
Z\sim N(0,1).
```

Переход к стандартной нормальной величине:

```math
Z=\frac{X-a}{\sigma}.
```

---

## 11. Закон больших чисел и центральная предельная теорема

### 11.1. Закон больших чисел

Закон больших чисел объясняет, почему среднее значение большого числа наблюдений приближается к математическому ожиданию.

Если `X_1,X_2,\dots,X_n` - независимые одинаково распределенные случайные величины с конечным математическим ожиданием `a`, то выборочное среднее:

```math
\overline X=\frac1n\sum_{i=1}^nX_i
```

при больших `n` приближается к:

```math
a=E(X).
```

В вероятностной форме:

```math
P(|\overline X-a|<\varepsilon)\to1,
\qquad n\to\infty.
```

### 11.2. Центральная предельная теорема

Центральная предельная теорема объясняет появление нормального распределения.

Если `X_1,\dots,X_n` независимы, одинаково распределены, имеют математическое ожидание `a` и дисперсию `sigma^2`, то:

```math
\frac{\sum_{i=1}^nX_i-na}{\sigma\sqrt n}
```

при больших `n` приближенно имеет стандартное нормальное распределение.

Эквивалентно:

```math
\frac{\overline X-a}{\sigma/\sqrt n}
```

приближенно распределено как `N(0,1)`.

---

## 12. Основы математической статистики

### 12.1. Генеральная совокупность и выборка

Генеральная совокупность - это все объекты, которые изучаются.

Выборка - это часть генеральной совокупности, по которой делаются выводы.

Если наблюдения обозначены:

```math
X_1,X_2,\dots,X_n,
```

то число `n` называется объемом выборки.

### 12.2. Выборочное среднее

Выборочное среднее:

```math
\overline X=\frac1n\sum_{i=1}^nX_i.
```

Оно является оценкой математического ожидания.

### 12.3. Выборочная дисперсия

Исправленная выборочная дисперсия:

```math
S^2=\frac1{n-1}\sum_{i=1}^n(X_i-\overline X)^2.
```

Почему делят на `n-1`: так получается несмещенная оценка генеральной дисперсии.

Неисправленная выборочная дисперсия:

```math
D_B=\frac1n\sum_{i=1}^n(X_i-\overline X)^2.
```

### 12.4. Эмпирическая функция распределения

Эмпирическая функция распределения:

```math
F_n(x)=\frac{n_x}{n},
```

где `n_x` - число наблюдений, меньших или равных `x`.

Она является статистическим аналогом теоретической функции распределения.

---

## 13. Оценивание параметров

### 13.1. Точечная оценка

Точечная оценка - это одно число, которым оценивается неизвестный параметр.

Например:

```math
\overline X
```

оценивает математическое ожидание.

```math
S^2
```

оценивает дисперсию.

### 13.2. Свойства оценок

Оценка называется несмещенной, если:

```math
E(\hat\theta)=\theta.
```

Оценка называется состоятельной, если при увеличении объема выборки она стремится к истинному параметру.

Оценка называется эффективной, если среди несмещенных оценок имеет наименьшую дисперсию.

### 13.3. Метод моментов

Идея метода моментов:

```text
теоретические моменты приравниваются к выборочным моментам.
```

Например, если параметр выражается через математическое ожидание, то можно положить:

```math
E(X)=\overline X.
```

И затем решить уравнение относительно неизвестного параметра.

### 13.4. Метод максимального правдоподобия

Пусть плотность или вероятность зависит от параметра `theta`.

Функция правдоподобия:

```math
L(\theta)=\prod_{i=1}^n f(X_i,\theta).
```

Оценка максимального правдоподобия выбирает такое значение `theta`, при котором наблюденная выборка наиболее вероятна:

```math
\hat\theta=\arg\max_\theta L(\theta).
```

Обычно удобнее максимизировать логарифм правдоподобия:

```math
\ln L(\theta).
```

---

## 14. Доверительные интервалы

### 14.1. Смысл доверительного интервала

Доверительный интервал - это случайный интервал, построенный по выборке, который с заданной вероятностью накрывает неизвестный параметр.

Если доверительная вероятность равна:

```math
\gamma=1-\alpha,
```

то `alpha` называется уровнем значимости.

### 14.2. Доверительный интервал для математического ожидания при известной дисперсии

Если генеральная совокупность нормальна, дисперсия `sigma^2` известна, то:

```math
\overline X-z_{\alpha/2}\frac{\sigma}{\sqrt n}
<
a
<
\overline X+z_{\alpha/2}\frac{\sigma}{\sqrt n}.
```

Здесь `z_{\alpha/2}` - квантиль стандартного нормального распределения.

### 14.3. Доверительный интервал для математического ожидания при неизвестной дисперсии

Если дисперсия неизвестна, используют распределение Стьюдента:

```math
\overline X-t_{\alpha/2,n-1}\frac{S}{\sqrt n}
<
a
<
\overline X+t_{\alpha/2,n-1}\frac{S}{\sqrt n}.
```

Здесь:

- `S` - исправленное выборочное среднее квадратическое отклонение;
- `n-1` - число степеней свободы.

---

## 15. Проверка статистических гипотез

### 15.1. Основные понятия

Статистическая гипотеза - это предположение о распределении или параметрах распределения.

Нулевая гипотеза:

```math
H_0.
```

Альтернативная гипотеза:

```math
H_1.
```

Уровень значимости:

```math
\alpha.
```

Это вероятность отвергнуть верную нулевую гипотезу.

### 15.2. Общий алгоритм проверки гипотез

1. Сформулировать `H_0` и `H_1`.
2. Выбрать критерий.
3. Задать уровень значимости `alpha`.
4. Найти наблюдаемое значение статистики критерия.
5. Найти критическую область или `p-value`.
6. Принять решение: отвергнуть или не отвергнуть `H_0`.
7. Записать вывод в терминах задачи.

### 15.3. Ошибки первого и второго рода

Ошибка первого рода: отвергли `H_0`, хотя она верна.

Вероятность ошибки первого рода:

```math
\alpha.
```

Ошибка второго рода: не отвергли `H_0`, хотя верна альтернатива.

Вероятность ошибки второго рода:

```math
\beta.
```

### 15.4. Критерий согласия Пирсона

Критерий Пирсона применяется, когда нужно проверить, согласуются ли наблюдаемые частоты с предполагаемым распределением.

Статистика:

```math
\chi^2_{\text{набл}}=
\sum_{i=1}^k\frac{(n_i-np_i)^2}{np_i}.
```

Здесь:

- `n_i` - наблюдаемая частота;
- `np_i` - ожидаемая частота;
- `p_i` - теоретическая вероятность попадания в `i`-й интервал;
- `k` - число групп.

Если параметры распределения оценивались по выборке, число степеней свободы уменьшается.

---

## 16. Корреляция и регрессия в статистике

### 16.1. Выборочный коэффициент корреляции

Для пар наблюдений:

```math
(x_1,y_1),\dots,(x_n,y_n)
```

выборочный коэффициент корреляции:

```math
r=
\frac{\sum_{i=1}^n(x_i-\overline x)(y_i-\overline y)}
{\sqrt{\sum_{i=1}^n(x_i-\overline x)^2}
\sqrt{\sum_{i=1}^n(y_i-\overline y)^2}}.
```

Он показывает силу линейной связи между признаками.

### 16.2. Линейная регрессия

Модель парной линейной регрессии:

```math
y=ax+b.
```

Коэффициенты `a` и `b` обычно находят методом наименьших квадратов, то есть минимизируют сумму квадратов отклонений:

```math
\sum_{i=1}^n(y_i-a x_i-b)^2.
```

Формулы:

```math
a=
\frac{\sum_{i=1}^n(x_i-\overline x)(y_i-\overline y)}
{\sum_{i=1}^n(x_i-\overline x)^2},
```

```math
b=\overline y-a\overline x.
```

---

## 17. Типичные ошибки

### 17.1. В задачах на таблицу распределения

1. Не проверяют, что сумма вероятностей равна `1`.
2. Путают строки и столбцы при нахождении распределений `X` и `Y`.
3. Считают `E(XY)` как `E(X)E(Y)` без проверки независимости.
4. Забывают, что корреляция считается через ковариацию.

### 17.2. В задачах на плотность

1. Не проверяют, что плотность неотрицательна.
2. Не проверяют, что полный интеграл равен `1`.
3. Неверно записывают функцию распределения вне области плотности.
4. Забывают, что справа от области плотности функция распределения должна быть равна `1`.

### 17.3. В статистике

1. Путают генеральную и выборочную дисперсию.
2. Забывают деление на `n-1` в исправленной выборочной дисперсии.
3. Неверно выбирают распределение: нормальное или Стьюдента.
4. В гипотезах формулируют вывод только математически, но не объясняют его в терминах задачи.

---

## 18. Краткий экзаменационный минимум

### Случайное событие

Событие - это подмножество пространства элементарных исходов.

Вероятность удовлетворяет аксиомам:

```math
P(A)\ge0,\qquad P(\Omega)=1,
```

и счетной аддитивности для несовместных событий.

### Условная вероятность

```math
P(A|B)=\frac{P(A\cap B)}{P(B)}.
```

### Формула Байеса

```math
P(H_k|A)=
\frac{P(H_k)P(A|H_k)}
{\sum_iP(H_i)P(A|H_i)}.
```

### Математическое ожидание

Для дискретной случайной величины:

```math
E(X)=\sum_i x_ip_i.
```

Для непрерывной:

```math
E(X)=\int_{-\infty}^{+\infty}xf(x)\,dx.
```

### Дисперсия

```math
D(X)=E(X^2)-[E(X)]^2.
```

### Функция распределения

```math
F(x)=P(X<x)
```

или:

```math
F(x)=P(X\le x).
```

Для непрерывной величины:

```math
F(x)=\int_{-\infty}^xf(t)\,dt.
```

### Коэффициент корреляции

```math
r_{XY}=
\frac{E(XY)-E(X)E(Y)}
{\sqrt{D(X)}\sqrt{D(Y)}}.
```

### Выборочное среднее

```math
\overline X=\frac1n\sum_{i=1}^nX_i.
```

### Исправленная выборочная дисперсия

```math
S^2=\frac1{n-1}\sum_{i=1}^n(X_i-\overline X)^2.
```

### Проверка гипотез

Общий порядок:

```text
H0, H1, уровень значимости, статистика критерия, критическая область, решение, содержательный вывод.
```

